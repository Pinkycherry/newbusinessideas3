# PENDING

- 2026-08-08 18:40 IST | `supabase: public.profiles` (RLS policy) | Dropped the client-writable UPDATE policy on `profiles` | Success | Found during audit: any signed-in user could run `update profiles set plan_tier='lifetime'` from the browser console and self-grant a paid plan for free — real revenue leak, closed immediately, nothing in the app relied on client-side profile updates.
- 2026-08-08 18:38 IST | `supabase: public.handle_new_user()` (function grants) | Revoked EXECUTE on the signup trigger function from `anon`/`authenticated` | Success | Was flagged by Supabase's own security linter as publicly callable via RPC; trigger still fires fine (doesn't need EXECUTE grant), just no longer directly invokable by outside callers.
- 2026-08-08 18:20 IST | none (process decision) | Razorpay work deprioritized | N/A | Founder call — approval process outside our control, not chasing revenue right now. Do not resume until founder explicitly brings it back up.
- 2026-08-08 18:20 IST | none (blocked) | UPI QR manual-payment flow not built | Blocked | Needs founder's UPI VPA + display name to encode in the QR; never provided.
- 2026-08-08 18:20 IST | none (clarification) | "Manual plan activation" is not a self-serve tool | Clarified | Works today only as: founder reports a payment, Claude runs the UPDATE directly via Supabase MCP. No admin UI exists. Flagging so this isn't assumed to be more automated than it is.
- 2026-08-08 19:45 IST | `src/components/site-shell.tsx` | Signed-in state indicator added to header/mobile menu (name + sign out) | Success | Closes the earlier "no visible signed-in state" gap, bundled into Step 5 as instructed.
- 2026-08-08 19:45 IST | `src/components/site-shell.tsx` | Header rebuilt: 4 dropdowns (Categories, Browse by type, Explore, Company), dark/light toggle moved from the floating dock into the header per Section 12.3's explicit placement requirement | Provisional | Founder has not yet supplied the reference dropdown-layout images (Section 12.5) or footer reference (12.4) — built sensible defaults now rather than blocking. Revisit structure once references arrive.
- 2026-08-08 19:45 IST | `src/components/site-shell.tsx` | Footer widened to 5 columns, added a live "Popular categories" column | Provisional | Same reason — no reference example supplied yet (12.4). Current version is a genuine width/content increase, not a placeholder, but may be restructured once the founder's reference lands.
- 2026-08-08 19:10 IST | none (blocked) | Migrate ethicalfounder.com/upcomingtools.com hotlinked images to our own storage | Low priority | Confirmed same-owner properties, no urgency — but hotlinking depends on another site's uptime/paths. Move under our own domain once image infra exists.
- 2026-08-08 18:20 IST | `src/hooks/use-auth.ts`, `src/components/idea-card.tsx`, `src/routes/idea.$slug.tsx` | Brief loading-state flash before session resolves | Doubtful, not fixed | During the async session check, gated content shows "locked" even for already-logged-in users for a moment. Cosmetic race condition, not a functional bug — logged for later polish.
- 2026-08-08 18:20 IST | none (open question, PROJECT_BRIEF.md Section 3.2) | Section 6.7 guide-page tier (public vs. gated) still undecided | Open | Doesn't block anything yet — Section 6.7 template isn't built (Build Order step 12). Needs a founder decision before that step starts.
- 2026-08-08 19:49 IST | `src/components/site-shell.tsx` | Fixed two real header bugs found by code review: ThemeToggle got a conflicting size className (both h-11 and h-9 present, undefined winner) on top of its own hardcoded size; full desktop nav (4 dropdowns + search + toggle + auth) appeared starting at 768px with no wrap/shrink handling, very likely overflowing at 768-1024px | Success | Founder reported header layout issues; live URL and local dev are both blocked in this sandbox (egress policy + private registry both return policy 403s, confirmed not transient) so this was found via code review, not a screenshot. Moved the full-nav breakpoint from md (768px) to lg (1024px); mobile menu now covers that range instead.
- 2026-08-08 19:49 IST | `src/components/site-shell.tsx` (CategoryMega) | Categories dropdown panel is viewport-centered (`fixed left-1/2 ... -translate-x-1/2`) while the 3 new dropdowns anchor directly under their own button | Flagged, not fixed | Pre-existing behavior (not something I changed), but now visually inconsistent next to 3 correctly-anchored dropdowns since Categories is no longer the only dropdown. Didn't guess-fix this one blind — needs an actual look (screenshot or founder's reference images) before changing positioning that was presumably deliberate.
- 2026-08-08 20:35 IST | `src/components/site-shell.tsx` (CategoryMega) | Fixed the flagged positioning inconsistency above — founder gave an explicit spec (absolute/top-full/left-0, anchored under its own button) | Success | Matches the pattern the other 3 dropdowns already used; no more viewport-fixed/centered panel.
- 2026-08-08 20:35 IST | `src/components/category-badge.tsx` (new file) | Extracted a shared `CategoryBadge` component, used in the header dropdown, footer "Popular categories", and Golden Tree nodes | Success | Founder-requested refactor so category-pill styling only needs editing in one place. Adapted to reuse the `iv-tag`/`iv-tag-cloud` classes already shipped on `main` (see next entry) rather than reinventing pill styling.
- 2026-08-08 20:35 IST | `src/routes/index.tsx`, `src/styles.css` | Golden Tree section: wrapped the tree artwork in a full-bleed dark radial backdrop (breaks out of the `max-w-6xl` container) and added a gentle float/drift animation to desktop nodes (`.bbi-tree-node-d`, mirrors the existing mobile `.bbi-tree-float`) | Success | Founder-requested overhaul; desktop nodes were previously static while mobile nodes already floated.
- 2026-08-08 20:35 IST | `src/components/site-shell.tsx`, `src/routes/index.tsx` (stray shadow) | Fixed one more stale orange `oklch` shadow (header/footer BBI logo pill) missed by the earlier color-mix sweep, and committed the 7-file color-mix fix from the earlier color audit that had been code-complete but sitting uncommitted | Success | Same root cause as the earlier sweep — a hardcoded old-palette orange glow left over on a button whose gradient is actually indigo/violet.
- 2026-08-08 20:35 IST | (process note, no file) | Found `main` had moved 3 commits ahead with unrelated, more mature concurrent work (`iv-nav-panel`, `iv-tag-cloud`, a re-tuned Golden Tree backing plate with light-mode support) — presumably pushed directly via Lovable, not through this branch | Reconciled | Rebased onto latest `main` instead of pushing blind; resolved the one real conflict (footer "Popular categories" markup) by hand so `CategoryBadge` renders through the already-shipped `iv-tag`/`iv-tag-cloud` classes instead of competing with them. Nothing from that concurrent work was overwritten. Worth knowing going forward: `main` can move without a PR from this branch, so check `git fetch origin main` before assuming this branch is current.
- 2026-08-08 20:35 IST | none (sandbox constraint) | Could not visually verify PR #7's Vercel preview before merging | Blocked, not fixed | `WebFetch` on the fresh preview URL returned `EGRESS_BLOCKED` (confirmed, not assumed) — same sandbox network policy as before. Merged on code-level review (rebase conflicts resolved by hand, `tsc`/`eslint` clean, CI/Vercel build green) per the live-check rule's fallback path; still needs an actual pixel check from the founder.
- 2026-08-08 21:15 IST | `src/routes/index.tsx`, `src/styles.css` | Golden Tree turned fully invisible after PR #10's `mask-image: url(...)` luminance-mask fix | Failure, reverted in PR #11 | Real mistake: `mask-image` needs to read the referenced image's actual pixel data, and both tree assets are hotlinked from `ethicalfounder.com` (a different domain), which sends no CORS headers — the browser blocks the pixel read and renders the masked element as fully transparent (nothing shows), on every device/theme, not just imperfectly. Reverted to `mix-blend-mode:screen` (a pure rendering composite, not a pixel read, so cross-origin-safe) — the tree renders again but transparency is back to an approximation, not pixel-perfect. Root cause note for next time: any pixel-reading CSS/SVG technique (masks, luminance filters, canvas reads) is a non-starter on a hotlinked cross-origin image unless that host adds CORS headers — should have checked this before trying it live.
- 2026-08-08 21:15 IST | none (needs founder action) | Durable fix for Golden Tree transparency | Open | Two real options, neither buildable from this sandbox: (1) get the tree artwork re-exported as a real PNG/WEBP with an actual alpha channel via the founder's AI-image-generation flow (Section 1's custom-asset pipeline) and host it — genuine alpha means no blend/mask hacks needed at all; (2) if staying hotlinked from ethicalfounder.com, that host would need to send `Access-Control-Allow-Origin` headers on the two tree image files so cross-origin masking becomes possible. Until either happens, `mix-blend-mode:screen` is the ceiling of what's achievable.
- 2026-08-08 21:35 IST | `src/routes/index.tsx` (GoldenTreeSection), `src/styles.css` (`.tree-asset-container`) | Golden Tree 3D-rendering/transparency polish — PENDING, paused | Paused per founder | Two attempts (boxed-frame fix, then a real-transparency mask) both required visual confirmation this sandbox cannot get (no live-URL/preview access — confirmed `EGRESS_BLOCKED` every time it's been tried), and the second attempt shipped a real regression (fully invisible tree) that only surfaced once the founder screenshotted it. Founder's call: stop iterating on this specific visual blind, mark it pending, move to the next Build Order step. Current state is the known-working `mix-blend-mode:screen` fallback (imperfect but visible, all devices/themes) — do not touch again without either a screenshot-verified round-trip or the founder explicitly asking. Any future attempt should assume zero live-preview access and treat unverifiable pixel-level visual claims as unconfirmed, not done.
- 2026-08-08 22:05 IST | `src/routes/index.tsx` | Founder asked for a Step 4/5/6 audit; found the Step 4 "brand name standardized site-wide" claim was false — the homepage's own route-level `head()` (overrides root meta) plus 5 more homepage spots still said "BBI — Best Business Ideas" instead of "Bharat Business Ideas", meaning the real rendered `<title>`/meta description on the live homepage was never actually corrected back in Step 4 | Fixed, then superseded | All 7 occurrences corrected to "Bharat Business Ideas" in this pass — see next entry, this name itself was then corrected again by the founder. Root cause note still stands: the original Step 4 fix touched `__root.tsx`'s defaults and `site-shell.tsx`'s logo, but never checked whether `index.tsx`'s own per-route `head()` was overriding those root defaults — grep the actual rendered output path (route-level overrides), not just the obvious shared components, whenever a brief entry claims "standardized site-wide."
- 2026-08-08 22:20 IST | `src/routes/__root.tsx`, `src/routes/index.tsx` | Founder correction: the confirmed brand name is **"BBI — Bro Business Ideas"**, NOT "Bharat Business Ideas" (which is what the brief incorrectly stated and this file's own previous entry just "fixed" everything to) | Fixed on root + homepage | Corrected in `__root.tsx` (site-wide default meta) and `index.tsx` (all 7 homepage spots). Repo-wide grep confirms zero "Bharat" occurrences left. **Standing instruction, not yet closed**: only `__root.tsx` and `index.tsx` were ever found spelling out the full name — every other route currently uses just the short form "BBI" in its own `head()`/copy, so nothing else needed a code change today. But whenever any other page is touched going forward (pricing, about, contact, blog, idea/category templates, sign-in, legal pages, new templates from Build Order steps 8/10/12, etc.), grep that file for "Bharat" and for any other spelled-out variant before shipping, and use "Bro Business Ideas" if the full name is ever written out. Do not close this PENDING item until a final repo-wide grep for "Bharat" turns up nothing across every file type (not just `src`), including `PROJECT_BRIEF.md`/`PENDING.md`'s own historical entries, which intentionally still say "Bharat" in past-tense log lines and should NOT be edited (they're a record of what was done at the time, not live copy).
- 2026-08-08 22:45 IST | `src/routes/index.tsx` (not yet touched), `src/components/site-shell.tsx` (not yet touched) | Full homepage copy rewrite drafted per founder's detailed direction (6th/7th-grade English, warm human tone, hide the AI-handoff mechanism from marketing copy) — full draft text is saved in `PROJECT_BRIEF.md` under Step 4, NOT in this file (too long for a bullet) | Approved and applied — see next entry | Superseded.
- 2026-08-09 12:15 IST | new: `src/components/cursor-trail.tsx`; deleted: `src/components/cursor-glow.tsx`; edited: `src/components/site-shell.tsx`, `src/styles.css` | Founder replaced the glow+dot cursor entirely with a different, precisely-specified technique — the classic "ghost trail" (1 face element + 19 plain trailing circles, chain-lag from a per-circle CSS transition-duration stagger, not lerp/RAF) — after sharing a reference CodePen (HAML/SCSS/JS), reskinned blue-violet from our own tokens instead of the reference's white/grey | Success | Deleted the glow+dot+footer-accent component and its CSS entirely, per "replacing... entirely, not running alongside it" — no footer-specific variant this time since the new spec doesn't call for one. New component: 20 elements total (1 lead face + 19 circles), size `100 - i*4`px, z-index `20-i`, transition-duration `i/20`s with the reference's cubic-bezier, centered via negative margin. All 20 circles get the *same* left/top target on `mousemove` (written directly via refs, no React state/re-render) — the staggered transition-duration alone produces the trailing chain, no animation loop needed, exactly as specified. Color interpolated per-circle via `color-mix(in oklab, var(--violet-soft) X%, var(--primary) Y%)` — token-driven, not hardcoded hex, consistent with the rest of this codebase. Face gets small eyes/mouth/waving-arms, kept subtle (low-opacity foreground token, small size). Guards: `(hover:hover) and (pointer:fine)` only (no touch fallback, per explicit "never on touch, full stop" this round), fully absent under reduced motion, hidden on tab-hidden, lazy-loaded (confirmed as its own build chunk). Verified visually again: ran locally, screenshotted mid-motion/settled/a close-up of just the face, and actually looked at all three — face renders cleanly with visible eyes/smile/arms, trailing circles show correctly as a chain during motion and collapse together when idle (matches the reference's actual behavior). `tsc`, `eslint`, `npm run build` all clean.
- 2026-08-09 11:50 IST | new: `src/components/cursor-glow.tsx`; deleted: `src/components/cursor-smoke.tsx`; edited: `src/components/site-shell.tsx`, `src/styles.css` | Founder rewrote the cursor spec with exact numbers after the first two attempts didn't land — full rebuild to match: glow (600x400 oval, blur(90px), lerp 0.08) + a separate tight-following 12px solid dot (lerp 0.3), idle autonomous drift after 1.5s (lissajous figure-eight, 22s loop), hover scale-up on interactive elements, footer intensity boost (opacity 0.42→0.65, lerp 0.08→0.1), desktop-only via `(hover:hover) and (pointer:fine)` (no touch fallback this time — founder was explicit "never on touch devices, full stop"), fully absent under prefers-reduced-motion, paused on tab-hidden/pointer-leaves-window, lazy-loaded (confirmed in build output as its own chunk, separate from site-shell's bundle) | Success, one deliberate deviation flagged before shipping, not after | **Deviation from the literal spec, disclosed up front**: spec said `z-index: 9999` for the global glow, reasoning "pointer-events:none means it can never block clicks." That's true for clicks but not for legibility — position:fixed elements with *any* explicit z-index paint above all normal unpositioned content regardless of the number (CSS stacking mechanics, not a bug), so literal 9999 would render the glow **over** card text exactly like the founder's own "worst, just scrap" reference screenshot showed. Used z-index:3 instead, which achieves the actually-stated goal ("above background, below all real content") — reasoning written as a code comment in styles.css, not just here. For the footer's stricter "must render behind text, isolation:isolate, z-index:-1" requirement, reused the exact pattern already proven working in this codebase for `.bbi-spotlight` (isolated stacking context + a `::before` at z-index:-1) rather than trying to make the single global fixed layer do double duty — a genuinely separate small accent element, sharing the same lerped cursor position translated into footer-local coordinates, portalless (just found via `document.querySelector('footer')` and prepended). **Verified visually this time, not just via DOM value checks**: ran the local dev server, drove headless Chromium, and actually looked at 3 screenshots (hero, footer, button-hover) with the Read tool — confirmed the glow renders soft and legible over hero text, renders correctly tucked behind footer text/links (all footer copy stayed crisp), and the dot visibly scales up over the "Browse the library" button. `tsc --noEmit`, `eslint`, and `npm run build` all clean. Per the founder's explicit "one section at a time, stop after each" instruction, only items 1+2 of their 17-item list were done this round — nothing else was touched.
- 2026-08-09 04:05 IST | new: `src/components/cursor-smoke.tsx`; deleted: `src/components/tilt-panel.tsx`; edited: `src/routes/index.tsx`, `src/components/site-shell.tsx`, `src/styles.css` | Founder feedback round 2 on the animation pass, direct and specific this time: remove the hero's 3D tilt entirely, add a full-page cursor-follow "smoke" trail (not scoped to one button) | Success, verified in a real browser | The hero's `TiltPanel` wrapper (mouse-driven `perspective`/`rotateX`/`rotateY` "bending" on the whole hero panel, which also dragged its 2 nested "What you get / How it works" cards along with it — that's almost certainly the "2-3 cards bending" the founder was also seeing) is removed entirely and the component file deleted; grepped the whole `src` tree for any other `rotateX`/`rotateY`/`perspective(` usage first — none, so this was the only tilt effect anywhere. New `CursorSmoke` component: 3 layered, heavily-blurred blue radial-gradient blobs (using the site's own `--primary`/`--accent` tokens, not a hardcoded color) that track the pointer via `pointermove` at the window level with staggered GSAP `quickTo` lag per layer (0.5s/0.85s/1.2s durations) for a trailing effect, mounted once in `SiteShell` so it's present on every page, `pointer-events:none` throughout, skipped entirely under `prefers-reduced-motion`. Uses `pointermove`/`pointerdown` (not `mousemove`) so a dragging finger on touch/tablet also drives it — flagged plainly to the founder that a static tap with no movement can't trigger it, since there's no such thing as "hover" on a touchscreen; that's an honest physical limit, not a bug.

  **Verification**: same real-browser method as the first pass — local dev server with a temporary, uncommitted Supabase mock (`db()` returns a chainable Proxy resolving `{data:[], error:null}` for local-only rendering, guarded behind `BBI_LOCAL_MOCK=1`, always reverted via `git checkout` immediately after use, confirmed absent from every commit) plus headless Chromium via Playwright. Confirmed directly in the DOM: hero panel's computed `transform` is `none` and it has no inline `style` at all after moving the mouse across it (tilt fully gone); 3 `.bbi-cursor-smoke-layer` elements exist, their computed `opacity` flips from faded to `1` on pointer movement, and their computed `transform` values track the moved-to coordinate with visible staggered lag between layers (e.g. one layer at `translate(700,500)` while the trailing layer sat at `translate(697.55,498.74)` a moment later) — i.e. the trailing effect is real, not just present in code. `tsc --noEmit` and `npm run build` both clean.
- 2026-08-09 03:40 IST | new: `src/lib/motion.ts`, `src/hooks/use-pill-interaction.ts`, `src/components/spotlight.tsx`, `src/components/card-fan.tsx`, `src/components/reveal.tsx`; edited: `src/components/ui/button.tsx`, `src/components/category-badge.tsx`, `src/components/site-shell.tsx`, `src/components/floating-dock.tsx`, `src/routes/browse.tsx`, `src/routes/index.tsx`, `src/styles.css` | Full animation-layer reset on GSAP, per founder's detailed spec | Success, with honest scope gaps below | GSAP (3.15, confirmed free incl. ScrollTrigger) installed and loaded lazily via `loadGsap()` (dynamic `import("gsap")`) so it lands in its own chunk, not the initial route bundle — verified in the production build output (`gsap-*.js` ~70KB standalone, separate from `index-*.js`). Every new effect checks `prefersReducedMotion()` before loading/animating and the spotlight glow also has a `@media (prefers-reduced-motion: reduce)` CSS fallback. Old scroll-reveal (IntersectionObserver + `.bbi-reveal` CSS classes) fully removed and replaced by `Reveal` (GSAP/ScrollTrigger). Old CSS-transition hover/press `transform` on `.glass-btn`/`.glass-pill`/`.sheen` removed and replaced by `usePillInteraction` (GSAP spring hover scale/lift + press squash) — wired into the shared `Button` component (now icon-forward via a new `icon` prop, always pill-shaped, no more square variant), `CategoryBadge` (header dropdown/footer/Golden Tree nodes — leaving Golden Tree's own art/backdrop untouched, only the pill wrapper), the new hero CTA and footer CTA, and `browse.tsx`'s subcategory pills. `Spotlight` (cursor-follow glow, `gsap.quickTo` on CSS custom properties) applied behind the hero CTA and the new footer CTA as specified. `CardFan` (shallow overlapping-arc reveal, straighten+scale+lift on hover, neighbors ease aside) built and applied to the homepage's four-pillar "Research Standard" section (desktop/tablet only — mobile keeps a plain stacked grid, a fanned arc needs room narrow viewports don't have). New "Built With" section added directly before the footer (site-wide, since footer is global chrome), 18 entries. Golden Tree and the twin rings were not touched at all, confirmed by re-reading both after the pass. `tsc --noEmit` and `eslint` clean on every touched/new file; a full `npm run build` succeeds.

  **Caught and fixed mid-pass**: removing the shared CSS `transform` rule broke hover motion on 2 buttons that had no independent hover effect of their own — `floating-dock.tsx`'s back-to-top and jump-to-section buttons (fixed with `whileHover`/`whileTap` on their existing `motion.button`, not GSAP, since mixing GSAP's direct inline-transform writes with framer-motion's own transform management on the same node risks the two fighting each other) and `browse.tsx`'s subcategory pill (fixed with the GSAP hook, no conflict there since it was a plain `Link`). Found by grepping every file using `glass-pill`/`glass-btn`/`sheen` after the CSS removal, not by guessing.

  **Honest scope gaps, not silently dropped**:
  1. lorolabs.ai (the named animation reference) could not be fetched — `EGRESS_BLOCKED`, consistent with this sandbox's known network policy. Everything above was built from the founder's own written description of the effects, not a pixel-match to the live reference. Worth a screenshot/reference-image round-trip later to check the fan-reveal's exact overlap/rotation feel and the spotlight's exact glow size against the real site.
  2. "Touches every button component, one pass, nothing left on the old style" is true for the shared `Button` component, `CategoryBadge`, and everywhere both are used — but 4 files still have their own standalone pill buttons using `sheen` + their own Tailwind `hover:scale-*` (not broken, just not migrated to the GSAP hook): `paywall-popup.tsx` (2 buttons), `validate-button.tsx` (2 buttons), `idea.$slug.tsx` (2 CTAs), `sign-in.tsx` (1 button). None of these lost functionality — they weren't touched by the CSS removal because they already carried their own hover effect — but they're inconsistent with the new motion system and should be migrated in a follow-up pass.
  3. Icon-forward: `Button` now accepts an `icon` prop and renders it before the label, but existing call sites across the site were not individually retrofitted with specific icons — picking the right icon per button across dozens of unseen route files is a content/design decision better done with visual review than guessed blind.
  4. Card fan-reveal was specified for "comparison section, team section, four-pillar section, testimonials" — only the four-pillar section (Research Standard) exists anywhere in this codebase today (grepped for all four, confirmed). The other three don't exist yet (comparison template is Build Order step 12, no team/testimonials section anywhere) — nothing to wire it into until those get built.
  5. "Built With" uses monogram-initial badges, not real brand logos — no licensed SVG logo assets exist in this repo for any of the 18 entries, and lucide-react (the only icon set here) doesn't carry brand marks. Flagged per Section 1 of the brief: needs real logo SVGs from the founder to look like the reference instead of initials.
  6. Founder asked for this as "its own PR, separate from the theme-removal PR already in flight" — no such PR was ever found anywhere in this repo (see the dark-mode-removal entries above), and this session's designated branch is fixed to `claude/bbi-continuation-sj6nbr` per its own setup instructions, so this landed as further commits on the same branch/PR (#17) as the skills addition and homepage copy, not a fresh PR. Flagging the mismatch with what was asked rather than silently deciding it didn't matter.
- 2026-08-09 02:55 IST | `src/components/theme-toggle.tsx` (deleted), `src/components/site-shell.tsx`, `src/routes/__root.tsx`, `src/styles.css`, `src/components/ui/alert.tsx` | Dark mode removed totally, per direct founder instruction ("remove dark mode totally, and related data too") | Success, with an open flag | Deleted `theme-toggle.tsx` and its 2 usages in the header/mobile menu; removed the pre-hydration theme script and `suppressHydrationWarning` from `__root.tsx` (no longer needed — nothing sets a class before paint now); removed the `iv-theme` localStorage key entirely (no code writes or reads it anymore); removed the `@custom-variant dark` declaration and the now-dead `.dark {}` block from `styles.css`; removed one leftover `dark:` Tailwind utility in `alert.tsx`. `chart.tsx`'s generic `THEMES = {light, dark}` shadcn primitive was left alone — it's a per-chart-config color mechanism, not page-level theming, and nothing in this codebase feeds it a dark variant. **Correction (same pass, caught before shipping further work)**: the note originally written here said `styles.css` had no separate light palette at all — that was wrong, found by only reading the first ~160 lines of a much longer file. There is a fully-built `html.light { ... }` block much further down (real white/near-white values, ~46 CSS rules keyed off that class) that the toggle used to apply. Since removing the toggle meant nothing ever added that class anymore, the site was about to default to the dark `:root` palette permanently — the opposite of "light-only." Fixed by statically setting `className="light"` on `<html>` in `__root.tsx` (no JS, no toggle, always on), so the real light palette that was already built now renders permanently. `:root`'s dark values are left in place as an inert fallback (documented with a comment) rather than deleted, since deleting them requires touching 46 cascade-dependent rules blind with no screenshot to verify against — leaving them costs nothing since `html.light` always wins the cascade. PROJECT_BRIEF.md Section 12.3 (dark/light toggle "mandatory, not optional") is now superseded by the founder's direct instruction — noting here rather than editing that historical section.
- 2026-08-09 02:40 IST | `src/routes/index.tsx`, `src/components/site-shell.tsx` | Founder approved the drafted homepage copy verbatim (footer tagline: option A). Applied all 8 named sections (hero eyebrow/H1/subtext, what-you-get panel, how-it-works panel, who-we-are banner, problem-we-found section, research-standard headline/sub/4 pillar descs, why-this-exists headline/3 paragraphs/4 sidebar rows, pricing stat note) plus the 3 founder-flagged bonus mechanism-leak fixes (feature card + 2 FAQ answers) plus the footer tagline (option A, added as its own line, existing copyright/live-data lines kept, not replaced) | Success | Also found and fixed 2 more mechanism-leak spots using the same standing principle (never say "we hand you a prompt" / name the destination AI account in marketing copy) that the founder didn't explicitly flag this round: the "Validate it, free" step in the secondary `HowItWorksSection` (`BBI_HOW_STEPS`), and the footer's product blurb, both of which said "hands you a prompt on your own Claude or Perplexity account." Rewrote both to "using AI tools you already pay for," matching the voice now used everywhere else. Deliberately left two "Claude or Perplexity" mentions untouched: one in the approved "problem we found" paragraph (founder's own verbatim wording, referencing subscription cost, not the handoff mechanism), and the FAQ "Which AI does the Validate button use?" (a direct, appropriate FAQ answer about supported platforms, not a mechanism explanation). Repo-wide grep confirms zero "Bharat" occurrences remain. `tsc --noEmit` clean, `eslint` clean (auto-fixed 8 prettier formatting nits, no logic/content changes from the fix). This also closes the standing "no pain/emotion, robotic tone" complaint on Section 11.1 brand voice.
- 2026-08-08 22:05 IST | `src/routes/category.$categorySlug.tsx`, `.index.tsx`, `.$subcategorySlug.tsx` | Section 12.7 ("no repetitive same-size card walls") not applied to the main idea/category browsing grids | Open, deliberately deferred | These are still a uniform `grid-cols-2/3` wall of identical `IdeaCard`s. Fixed this pattern on the blog index (PR #13, editorial context, safe) but did not touch the idea grids — this is the site's primary comparison-heavy browsing UI, and forcing visual size variation onto it without live verification is a real usability risk, not just a visual polish call. Needs either explicit founder direction on what variation should look like, or a screenshot round-trip, before attempting.
- 2026-08-09 21:43 IST | `src/routes/browse.tsx` | Mobile bug: category heading collided with the idea-count number and rendered pill-styled instead of plain text | Success | Founder reported with a real screenshot. Root cause was two separate things stacking: a global `a[href*="/category/"]` rule pill-styled the plain category-heading link, and a non-wrapping `flex justify-between` row let a long heading push straight into the count. Fixed by using the already-existing `.iv-plainlinks` escape hatch (built for this exact purpose elsewhere, just not applied here) on the heading link, and switching the row to `flex-col` on mobile / `sm:flex-row` on larger screens. Verified with Playwright against a synthetic long category name at a 390px viewport — heading now wraps in plain dark text with no overlap.
- 2026-08-09 21:43 IST | `src/components/cursor-trail.tsx`, `src/styles.css` | Cursor companion redesigned per founder feedback ("laggy", wanted a "chubby" character with tiny hands/legs/tail, wanted it interactive, wanted dynamic motion instead of a static glide) | Success | Root-caused the reported lag to two real perf issues: an infinite CSS animation running on all 20 trail circles at all times regardless of interaction, and an unthrottled `mousemove` handler writing to 20 DOM elements on every raw event (can exceed 60/sec). Removed the always-on animations entirely (now purely event/timeout-driven, zero cost at idle) and rAF-throttled the position writes to once per rendered frame. Added tail + leg elements to the face for a rounder, more character-like silhouette. Added real interactivity: the character now squashes/stretches along its direction of travel based on actual pointer velocity while moving (the "dynamic motion, not a static glide" ask), and after 1.2s of sitting idle over empty space it points an arm at the nearest on-screen interactive element instead of just facing the viewer — it only does this over empty space, not when already on top of something clickable. Verified via local Playwright screenshots (squash-stretch mid-sweep, idle-pointing at a real button, both before and after adding the rAF throttle). Desktop-with-real-cursor only, absent under `prefers-reduced-motion` and on touch devices, as before.
- 2026-08-09 21:43 IST | `src/routes/index.tsx` | Founder reported "no visible animation on mobile" | Success | The existing scroll-triggered `Reveal` component (`rv-lift` variant) isn't hover-dependent, so it already works on touch — it just wasn't wrapped around most of the homepage. Wrapped the ~11 previously-bare sections below the hero (live demand tracker, brand statement, keyword mosaic, trust stats, how-it-works, who-for, pricing philosophy, why-we-built-this, team, inspired-by, comparison, future-proof spotlight, promise section) in `<Reveal variant="rv-lift">`, matching the treatment already used elsewhere on the page. `tsc --noEmit` clean.
- 2026-08-09 21:43 IST | `src/styles.css` | Mobile bug: long subcategory/category pill labels were rendering with characters silently cut from both the start AND the end, no "…" shown | Success | Founder reported with real screenshots ("I need all of these with proper text, without missing any single character or word"). Root cause: `.iv-tag-cloud .iv-tag` used `white-space:nowrap; overflow:hidden; text-overflow:ellipsis`, but `.glass-pill` (which every `.iv-tag` also is) sets `justify-content:center` — browsers don't reliably fire `text-overflow:ellipsis` on a horizontally-centered flex child, so the overflow was just silently clipped from both edges instead. Fixed by letting long labels wrap onto a second line inside their own pill (`white-space:normal; overflow:visible`) instead of truncating at all, so the complete label is always visible, on any screen width. Verified with Playwright using the founder's own long subcategory names (391px viewport) — every character now shows, wrapped, no clipping — and re-verified desktop stays single-line/unchanged since short labels never need to wrap. This CSS class is shared by footer/nav-dropdown pills too; those still render exactly as before since none of their labels are long enough to trigger wrapping.
- 2026-08-09 21:43 IST | `src/components/site-shell.tsx`, `src/styles.css`, `package.json` | "Built With" section redesigned into a horizontal auto-scrolling marquee of real, clickable, outbound brand links, closing the gap flagged 2026-08-08 ("no licensed SVG logo assets... needs real logo SVGs from the founder") | Success, with one disclosed, deliberate gap | Founder called the old static monogram-initial grid "totally 3rd class." Replaced it with a seamless-loop marquee (same doubled-list + `translateX(-50%)` technique as the homepage's existing category ticker, edge-masked, pauses on hover, collapses to a static wrapped row under `prefers-reduced-motion`) where every one of the 18 entries is now a real `<a target="_blank" rel="noopener noreferrer">` to that technology's real official site (react.dev, tailwindcss.com, supabase.com, etc. — all top-level canonical domains, nothing guessed past the root). 16 of 18 entries render their real brand mark from the licensed Simple Icons set (added `react-icons`, tree-shaken — confirmed via build output it lands in its own ~28KB chunk, not the main bundle). Icons render in one flat accent tone rather than each brand's exact hex, on purpose — guessing brand colors from memory risked showing an inaccurate color, which is exactly the kind of thing this founder has asked not to happen. **Disclosed gap**: ChatGPT and Grok have no icon here, not by oversight — Simple Icons removed both marks after trademark disputes with OpenAI/xAI, so there is no licensed asset for either; those two keep a plain text label rather than a fabricated logo. Verified via Playwright (desktop + mobile, two frames 1.5s apart confirming the strip actually moves, all 18 hrefs/target/rel attributes checked programmatically). `tsc --noEmit` and `eslint` both clean; `npm run build` succeeds with `react-icons` correctly code-split.
- 2026-08-09 22:40 IST | `src/styles.css` | Real bug (not a caching issue): the browse-page category heading was STILL rendering as a bordered/pill-shaped box after the previous `.iv-plainlinks` fix, confirmed by founder screenshot and reproduced locally with computed-style inspection | Success | Root cause was CSS cascade layers, not specificity: `html.light a[href*="/category/"] { ...!important }` lives inside `@layer utilities` (added for the light-mode-only rewrite), and per spec, an `!important` declaration inside ANY named layer always outranks an `!important` declaration that isn't in a layer, regardless of selector specificity. My earlier `.iv-plainlinks a[href*="/category/"]` override was unlayered, so it silently lost every time despite matching visually in my own dev-server screenshot (the border color there was pale enough — `rgba(43,40,113,0.16)` on a near-white background — that I didn't visually catch it in a downscaled screenshot; the founder's real device made it obvious). Fixed by moving the override into `@layer utilities` itself and adding an `html.light`-prefixed duplicate so its specificity also exceeds the rule it needs to beat. Verified by reading the computed `border`/`background`/`border-radius` directly via Playwright (not just eyeballing a screenshot this time) — confirmed `none`/`0px` after the fix, both before and after.
- 2026-08-09 22:40 IST | `src/routes/browse.tsx` | Founder: browse page "too messy without any logic... literally 3rd class... category names also too worst" | Success | Wrapped each category into its own `glass glass-hover` card (rounded-3xl, real padding) instead of a bare heading + bottom-border, so categories read as distinct grouped sections instead of one continuous list. Category heading is now plain bold text with a hover-color transition (no pill shape at all, addressing "looks the same as the actual business idea buttons below"); the idea count moved into a small `bg-primary/10` badge — visually a metadata tag, not a button (no link, no hover motion). Verified with Playwright at both 390px and 1440px against realistic long category/subcategory names matching the founder's own screenshots.
- 2026-08-09 22:40 IST | `src/components/ui/select.tsx`, `src/routes/index.tsx` | Founder: Surprise Me's category filter opened "a generic and low class ui" | Success | It was a plain native `<select>`, so tapping it handed control to the OS's own picker UI (the plain white radio-button list in the founder's screenshot) — that's the browser, not this codebase, and can't be reskinned while it stays a native `<select>`. Replaced it with the already-installed-but-unused shadcn/Radix `Select` (`src/components/ui/select.tsx` had default shadcn styling and zero call sites anywhere in the repo, so reskinning it globally was safe) restyled to match the site's glass aesthetic: trigger is a `.glass-pill` with a rotating chevron, the popover is a `.glass` card with rounded items and an accent-colored checkmark on the active one. Verified via Playwright, open and closed, mobile and desktop.
- 2026-08-09 22:40 IST | none (clarification, no code change) | Founder screenshot showed the OLD static monogram-grid Built With section, not the new marquee shipped in the previous entry | Not a bug | Re-tested the exact current code locally (mobile + desktop, real logos, marquee actually scrolling between two frames 1.5s apart) — it works. The founder's screenshot's timestamp and the plain `.vercel.app` URL in the address bar (not the branch-specific preview URL) both point to a stale tab/deployment predating that push, consistent with the "manually promoted production URL doesn't auto-update" behavior already flagged earlier this session. No code fix applicable here; flagging so this isn't mistaken for a regression later.
- 2026-08-09 22:48 IST | `src/styles.css`, `src/routes/browse.tsx`, `src/routes/category.$categorySlug.index.tsx` | Founder: "better glow animation on categories headings with our brand colors" | Success, but see next entry — this shipped a real perf regression | Added a breathing text-shadow glow (`.bbi-heading-glow`, 3.4s ease-in-out, brand violet/primary at pulsing intensity) to every place a category NAME is the heading: each browse-page category card and the individual category page's H1. Matches the pulsing-intensity technique already used by the orbit ring/dot glows elsewhere in this file rather than inventing a new motion language. Disabled (falls back to a static glow) under `prefers-reduced-motion`, same as every other animation this session. Hit the exact same `a[href*="/category/"]` collision as the pill-border bug two entries up — that selector also sets `animation: none !important`, which silently killed the glow on the browse-page card headings (they're `<a>` links) until the override repeated the attribute selector for higher specificity; the category-page H1 was never affected since a bare `<h1>` has no `href` to match. Verified the animation is actually running via computed `animationName`/`text-shadow` values, not just a screenshot, given the same class of bug already slipped through a visual-only check once this session. **What I did not check at the time: whether it was cheap to run.**
- 2026-08-10 00:25 IST | `src/styles.css` | Founder reported severe lag/failure to load on a real low-end mobile device, and (separately) doubted whether anything had actually been pushed | Fixed one real regression; partially explained, not fully resolved | Two separate things here. (1) **Trust**: confirmed `git rev-parse HEAD` on this branch exactly matches `origin/claude/bbi-continuation-sj6nbr` (commit `b24dd18`, 13 commits) and matches what `pull_request_read` reports as PR #17's head sha — nothing local/uncommitted, nothing fabricated; this is independently checkable by anyone with repo access. (2) **Performance**: simulated a weak Android phone with Playwright (6x CPU throttle + slow-4G network emulation via CDP) rather than assume. Found a real, self-inflicted regression from the immediately preceding entry: the category-heading glow animated the `text-shadow` property directly between two states with different blur radii — animating shadow blur is not compositor-only, the browser repaints the blurred shadow bitmap every frame, on every heading, forever. Confirmed via `PerformanceObserver` long-task entries while scrolling: 8 main-thread-blocking tasks (56–72ms each) before the fix, 0 after. Fixed by making the text-shadow itself static (computed once) and animating only `opacity` (compositor-only) for the same breathing effect — same visual result, near-zero cost. Audited every other `@keyframes` block in this file for the same anti-pattern (animating box-shadow/blur/background-position in an infinite loop) — everything else already only animates `transform`/`opacity`, consistent with this file's existing convention; the glow was the one rule that broke it. **What this does NOT fully explain**: total page load under the simulated bad network was still 15–20s, and 5 render-relevant images (3 editorial photos, both Golden Tree images) are hotlinked from a third-party WordPress site (`ethicalfounder.com`), not this app's own CDN, not resized/optimized, and one of them (the desktop Golden Tree hero) loads eagerly, above the fold, with no `loading="lazy"`. That domain is unreachable from this sandbox (proxy-blocked), so I could not confirm real file sizes or rule out WordPress-side hotlink protection (a very common setup that blocks/403s image requests whose `Referer` isn't the WordPress site's own domain — which is exactly the request pattern a visitor's browser sends here, and would look precisely like "broken/failed to load" if true). Flagging as the next concrete thing to check — ideally the founder opens browser DevTools → Network tab on the live site and reports whether any `ethicalfounder.com` image request comes back non-200, or whether the hero image is visibly missing/broken, since I cannot see that from here. The `index.tsx` comment locking these two tree URLs ("do not add or swap in others") means I did not touch them without that confirmation.
- 2026-08-10 00:57 IST | none (session paused, founder stepping away for a few hours) | Consolidated open-items note per founder's request ("keep a note, I will come back in few hours") | N/A — status snapshot | Everything committed and pushed as of `a319930` (verified `git rev-parse HEAD` == `origin/claude/bbi-continuation-sj6nbr`), all on PR #17 (still draft). Open items, in priority order:
  1. **[BLOCKING ON FOUNDER] Hotlinked-image hypothesis unconfirmed** — see entry directly above. Need the founder to check on the actual slow device (or DevTools Network tab) whether the Golden Tree / editorial images are visibly broken/missing, or just slow. This is the most likely remaining cause of "unable to load" beyond the glow-animation fix already shipped.
  2. **PR #17 is still draft, unmerged.** Nothing here has been merged to `main` yet — everything is live only on the Vercel preview URL for this branch, not on whatever URL is treated as "production." A PR check-in loop (`send_later`, ~hourly) is still active and will keep watching CI/mergeability/comments on its own; no action needed there while away.
  3. **Section 12.7 ("no repetitive same-size card walls")** never applied to the main idea/category browsing grids (`category.$categorySlug.tsx`, `.index.tsx`, `.$subcategorySlug.tsx`) — deliberately deferred, needs founder direction on what visual variation should look like before attempting (flagged 2026-08-08, still open).
  4. **The original 17-item fine-grained animation list** from earlier this session (hero fade-in-on-load, panel slide-ins, SurpriseMe clip-wipe transition, orbit-diagram entry/idle-bob/hover-tilt, FAQ accordion height-animation, trust-stats count-up, comparison-section slide-in-both-sides-with-glow) is still largely undone beyond the cursor work (superseded) and the generic homepage `Reveal` wraps. Founder has not restated interest in these specific items since pivoting to Built With / browse-page / lag issues — not resuming without a signal these are still wanted.
  5. **ChatGPT and Grok have no real logo** in the Built With marquee (Simple Icons dropped both marks after trademark disputes) — plain text label kept instead of a fabricated icon. Only revisit if the founder provides a licensed asset.
- 2026-08-10 17:58 IST | `src/lib/utils.ts` (new export), `src/routes/index.tsx`, `src/components/hero-slider.tsx` | Founder authorized autonomous work on the open items list ("complete all the pending... put it in autopilot"); item 1 was the hotlinked-image hypothesis, which was blocked on the founder's device confirmation — proactively hardened everything that doesn't require that confirmation | Success, found a real bug along the way | Also found a bigger instance of the same risk that wasn't in the original note: `hero-slider.tsx` (the actual first-visible hero panel, not just the Golden Tree section) uses 2 of the same `ethicalfounder.com` URLs and had `loading="lazy"` on its first slide — lazy-loading the one image that's always immediately visible on load actively deprioritizes the site's own LCP element; fixed to `loading="eager"`/`fetchPriority="high"` for slide 1 only (later slides correctly stay lazy). Added an `onError` fallback (hide the broken img, no CLS since every one of these already sits in a fixed-aspect-ratio container) to all 6 hotlinked `<img>` spots (3 editorial photos, 2 Golden Tree images, the hero slider). **First attempt at the onError fallback silently didn't work** — caught by testing against the real failure mode instead of trusting the code read: this app is server-rendered, and these `<img src>` tags start fetching as soon as the static HTML is parsed, before React hydrates and attaches the `onError` listener; since the sandbox's proxy fails these hosts near-instantly (`ERR_TUNNEL_CONNECTION_FAILED`), the browser's `error` event was firing and resolving before hydration could attach a handler — and a browser event that already fired never replays for a listener added afterward. Root-caused via an isolated non-React repro (proved raw `onerror=""` works against the same failing domain) plus a manual `complete && naturalWidth===0` check that showed 3 of 6 images were already in the failed state before any handler could have run. Fixed with `hideImgIfBroken` (`src/lib/utils.ts`), a ref-callback (not just an `onError` prop) that checks `complete && naturalWidth === 0` on every mount — ref callbacks fire during hydration too, so they catch the pre-hydration-failure case the `onError` prop alone misses; `onError` is still attached for genuinely later failures (e.g. a lazy image that fails once scrolled into view). Verified end-to-end against real failing requests (not mocked): all 6 spots now show `display:none` with no broken-image icon and no layout shift, confirmed via computed styles and a screenshot. `tsc --noEmit`/`eslint` clean, `npm run build` succeeds. Did not touch the two tree image URLs themselves (`index.tsx`'s "do not add or swap in others" comment) — this only ever runs when the existing `src` already failed to load, nothing content-related changed.
- 2026-08-10 17:05 IST | `src/components/magic-cursor.tsx` (new), `src/components/cursor-trail.tsx` (deleted), `src/components/site-shell.tsx`, `src/styles.css` | Founder reported the cursor mascot was directly covering/hiding a pill button, and asked for real named alternatives instead of another unilateral redesign — screenshot showed a "proto...s" pill fully hidden behind the character | Success | Root-caused with a fresh Explore pass rather than guessing: (1) a real z-index bug — `.bbi-cursor-trail` (fixed, page-wide) set `z-index: 3`, but `.glass-pill` never set its own z-index (`isolation: isolate` alone doesn't grant one), so the cursor layer unconditionally painted over any button lacking z-index ≥ 4, regardless of DOM order; (2) the lead "face" circle was also a 96×96px fully-opaque solid shape centered exactly on the pointer, large enough to fully cover a pill button by itself even with z-index fixed. Clicks were never blocked (`pointer-events: none` was already correct) — purely a visual-coverage bug. Rather than patch the mascot again, offered the founder 4 real, named options (fix-the-mascot / ring-dot "magic cursor" / spotlight-glow-only / magnetic-buttons) via AskUserQuestion; founder chose the ring/dot follower. Implemented as a full replacement: `magic-cursor.tsx` renders a small solid dot (8px, near-instant) plus a larger hollow ring (36px border, ~0.45s lag) via `gsap.quickTo` on `x`/`y` transform — the same technique already proven in `spotlight.tsx`, not a new pattern. Ring is deliberately hollow (transparent center) so it structurally cannot occlude button text the way a filled shape could, whatever the z-index. Hover state (scale to 1.55, border color shift on `.glass-pill`/`a`/`button`) is driven by `gsap.to({scale:...})` composed into the same GSAP-owned transform, not a CSS `width`/`height` transition — caught myself about to repeat the exact text-shadow/box-model animation mistake fixed two entries up (animating layout-affecting properties in a loop instead of compositor-only ones) and corrected it before shipping, not after. Dropped intentionally: the mascot's idle "point at nearby button" gesture has no equivalent on a ring — the hover scale/color morph is the standard replacement signal for "this is clickable" in this pattern. Verified via Playwright: hovering directly over a real `.glass-pill` now shows its full text through/around the ring (screenshot-verified, the exact regression this fixes); a fast pointer sweep shows visible dot-leads-ring separation (the trail effect); computed `transform` matrix confirmed scale actually reaches 1.55 on hover; confirmed absent under `prefers-reduced-motion` and on touch/mobile emulation, same gates as before. `tsc --noEmit`/`eslint` clean; `npm run build` succeeds with `magic-cursor` in its own code-split chunk (`magic-cursor-*.mjs`), same as the file it replaced. Also answered a separate, no-code question in the same round: `newbusinessideas3.vercel.app` (production alias) still shows old UI because it only follows `main`, and everything this session is on the still-open/draft PR #17's branch — expected Vercel behavior, not a deploy bug, nothing to fix in code.
- 2026-08-10 18:03 IST | `src/components/idea-card.tsx`, `src/routes/category.$categorySlug.index.tsx`, `src/routes/category.$categorySlug.$subcategorySlug.tsx` | Item 3 from the open-items list — Section 12.7 ("no repetitive same-size card walls") never applied to the idea/category grids, deliberately deferred since 2026-08-08 for lack of a spec; founder's "complete all the pending... autopilot" authorized proceeding on judgment | Success | Mirrored the pattern already proven safe on the blog index (PR #13) rather than inventing a new one: `IdeaCard` takes an optional `featured` prop that spans 2 grid columns (`sm:col-span-2`, cascades to the `lg:grid-cols-3` breakpoint too since nothing overrides it there) with larger title type (`text-2xl` vs `text-lg`), a longer summary clamp (4 lines vs 3), and more visible tags (5 vs 3). Differs from blog's "just the first card" rule since these grids can run to dozens of ideas, not 3 posts — used `i % 7 === 0` at both call sites (category page, subcategory page) so a highlight recurs roughly every 7 cards down a long grid instead of only once at the very top. Defaults to `false`, so every other existing caller of `IdeaCard` (search results, homepage trending/surprise sections, related-ideas on the idea detail page) is unaffected — didn't touch those, they weren't in the founder's flagged file list and weren't asked for. Verified with Playwright against a 14-item mock grid: desktop screenshot shows cards at index 0 and 7 visibly wider with larger type, breaking up the wall as intended; mobile screenshot confirms single-column layout is unaffected (col-span only applies at `sm:` and above, correctly a no-op below that). `tsc --noEmit`/`eslint` clean, `npm run build` succeeds.
- 2026-08-10 18:23 IST | `src/components/accordion-item.tsx` (new), `src/components/count-up.tsx` (new), `src/routes/index.tsx`, `src/styles.css` | Item 4 from the open-items list — the original 17-item fine-grained animation list from earlier in this session, largely undone. The exact original wording no longer exists anywhere in the repo (only my own paraphrase survived compaction), so each item below was implemented from its name against this codebase's own established conventions rather than a lost spec | Mixed — most already existed, several implemented, all verified | **First, corrected my own earlier note**: "hero fade-in-on-load" and "panel slide-ins" were already fully implemented (the hero H1/subtext/slider already use `.iv-fade-up` with staggered delays; the 4 scroll-stack panels already cycle through `Reveal` variants including `rv-slide`) and "orbit-diagram entry" already existed (`bbi-node-in`) — my prior PENDING.md entry calling these "still undone" was stale/wrong; didn't re-implement anything that already worked. What was genuinely missing, implemented this round: (1) **FAQ accordion height-animation** — all 4 FAQ blocks used native `<details>/<summary>` (instant, unanimatable open/close); replaced with a new shared `AccordionItem` component (button + CSS `grid-template-rows` 0fr→1fr transition, the standard technique for animating to an unknown/auto height without JS-measuring `scrollHeight`) used at all 4 call sites. One-shot, user-triggered ~300ms transition, not a continuous loop, so this doesn't repeat the animate-a-layout-property mistake from two entries up — that concern is about things that run forever. (2) **Trust-stats count-up** — new `CountUp` component, same `loadGsap(true)` + `ScrollTrigger{once:true}` engine already proven in `Reveal`, tweens a plain object and writes the formatted result straight to a ref's `textContent` (no React state per frame). (3) **Orbit-diagram idle-bob + hover-tilt** — neither could go on `.bbi-orbit-node-inner` (already owns a continuous counter-rotation `transform` via `bbi-orbit-spin-rev`; two CSS animations can't cleanly share one element's `transform`), so the bob got its own new wrapper element (`.bbi-orbit-node-bob`, translateY only) inserted between the outer node and the inner counter-rotator; the tilt went on `.bbi-orbit-node-label` instead, since that element only ever animates `opacity` (`bbi-label-fade`), so a static `:hover` transform there doesn't fight a running animation. (4) **SurpriseMe clip-wipe transition** — `Reveal`'s existing `rv-wipe` variant is ScrollTrigger-driven and doesn't fit content that appears via a mutation instead of a scroll, so this fires the identical `clipPath: inset(...)` technique directly via `gsap.fromTo` when `surprise.data` changes. (5) **Comparison-section slide-in-both-sides-with-glow** — built locally (not through `Reveal`) since the glow-on-arrival step is specific to this section, not something every `Reveal` caller wants: a `gsap.timeline` + `ScrollTrigger{once:true}` slides the left card in from `-60px`/right card from `+60px` simultaneously, then pulses a brief `boxShadow` glow before settling. Caught a real bug in my own first draft before shipping: the left card has a deliberate resting `opacity-80` (dimmed relative to the highlighted right card) — animating both cards TO `opacity:1` would have silently erased that intentional contrast; fixed to animate each card to its own resting opacity. Verified every item with Playwright screenshots against real interaction (FAQ click open/close confirmed via `aria-expanded` + screenshots showing the height genuinely animating; count-up confirmed via 3 timed frames showing 9→14, 626→967, 1→2; orbit hover-tilt confirmed via a visible border-highlight appearing only on `:hover`; comparison section confirmed via before/after screenshots with no visual defect in the settled state; SurpriseMe clip-wipe required fixing the local mock's `rpc()` stub, which was returning an empty array and silently hiding the results grid entirely — not a real bug, a test-mock gap, fixed to return real rows so the effect could actually be seen, then reverted before commit like always). `tsc --noEmit`/`eslint` clean, `npm run build` succeeds.
- 2026-08-14 09:07 IST | `src/components/magic-cursor.tsx` (deleted), `src/components/site-shell.tsx`, `src/styles.css` | Founder: after multiple redesign rounds (mascot → ring/dot), decided to remove the custom cursor entirely and handle its design themselves later — "put it normal, like a regular cursor" | Success | Removed the `MagicCursor` lazy import/render from `site-shell.tsx` (and the now-unused `lazy`/`Suspense` React imports along with it — nothing else in that file used them), deleted `magic-cursor.tsx`, and removed the `.bbi-cursor*` CSS block from `styles.css`. Site now uses the plain OS/browser cursor everywhere, no custom cursor layer at all. Confirmed no remaining references anywhere in `src/` (grepped for `magic-cursor`, `MagicCursor`, `bbi-cursor`). `tsc --noEmit`/`eslint` clean, `npm run build` succeeds.
- 2026-08-14 (multi-agent QA round, Agent 3) | `eslint.config.js` | Full QA/bug-hunt pass over all 20 commits on this branch (verification only — read every diff, re-ran `tsc`/`eslint`/`build` fresh rather than trusting past entries). Most of it came back clean, documented below, but found one real, in-scope bug | Success | **The bug**: commit `8965334` ("Add third-party marketing skills") added 37 `.ts` files under `.claude/skills/` (standalone Claude Code skill reference scripts, already excluded from `tsconfig.json`'s `src/**` type-checking, but never added to `eslint.config.js`'s `ignores`). Since then, a plain `npx eslint .` has reported 650 errors — every single one from those third-party scripts, none from this app's own `src/`. That silently made "eslint clean" a meaningless claim for every commit after `8965334`; this session's own PENDING.md entries kept asserting `eslint` clean on individual touched files, which was true, but a full-repo `eslint .` was not actually clean the whole time. **Fix**: added `.claude/skills` to the `ignores` array in `eslint.config.js`, next to the existing `dist`/`.output`/`.vinxi` entries, with a comment explaining why. Drops `npx eslint .` from 650 problems to 34 — confirmed every one of the remaining 34 is in a file this branch has never touched (`AdSlot.tsx`, `live-search.tsx`, `page-layout.tsx`, `ui/badge.tsx`, `ui/form.tsx`, `ui/navigation-menu.tsx`, `ui/sidebar.tsx`, `ui/toggle.tsx`, `config/collections.ts`, `config/featured.ts`, `hooks/use-auth.ts`, `lib/blog-shared.ts`, `lib/blog.functions.ts`, `lib/blog.server.ts`, `lib/schema.tsx` — via `git diff origin/main..HEAD --stat`, all show zero changes), i.e. pre-existing lint debt on `main` from before this session, correctly left alone rather than scope-creeping into an unrelated cleanup. **Everything else checked and confirmed clean, nothing else fixed**: audited every `@keyframes`/`gsap.to/from/timeline` call touched this session for the animate-a-non-compositor-property mistake (text-shadow/box-shadow/width/height/background-position) that bit this session twice before — found only two one-shot exceptions (`ComparisonSection`'s arrival glow animates `boxShadow` once via `ScrollTrigger{once:true}`, SurpriseMe's reveal animates `clipPath` once on mutation), both correctly gated to fire once, not in a loop, so not the same bug class; re-verified the `.bbi-heading-glow`/`.iv-plainlinks`/`html.light` cascade-layer fix live via Playwright (computed styles confirm pill styling gone + glow animation running simultaneously on the browse-page category heading); grepped for `magic-cursor`/`MagicCursor`/`bbi-cursor`/`cursor-trail`/`tilt-panel` — zero matches, fully removed, no orphaned imports; every `onError` usage is paired with the `hideImgIfBroken` ref-callback fix, verified live against real failing hotlink requests (both the pre-hydration and slower in-flight failure cases correctly end up hidden, no broken-image icon); `prefers-reduced-motion` gating present on every new animation entry point; read `idea-card.tsx`, both category route files, `accordion-item.tsx`, `count-up.tsx`, the full `routes/index.tsx` diff, and `lib/utils.ts` in full — no logic bugs found. `tsc --noEmit` clean, `npx eslint eslint.config.js` (the only file changed) clean, `npm run build` succeeds. Verification used the repo's local-mock+Playwright pattern (`BBI_LOCAL_MOCK=1` against a temporary Proxy mock in `db()`), reverted via `git checkout -- src/lib/ideas.functions.ts src/routeTree.gen.ts` before committing, confirmed absent from this commit. **Flagged, not fixed** (outside this round's scope per `MULTI_AGENT_WORKFLOW.md` Agent 3 = `eslint.config.js` only): `src/components/ambient-scene.tsx` (untouched by any of the 20 commits, confirmed via `git diff`) has two hardcoded `border-[oklch(0.723_0.161_56)]` / `border-[oklch(0.805_0.135_65)]` decorative ring accents — warm/orange hues that don't match the current all-indigo/violet brand palette, rendering globally on every page via `SiteShell`. Reads like the same class of leftover-old-palette bug this session's earlier color-mix sweep (2026-08-08) already fixed once elsewhere, just missed in this file. Not touched this round — belongs to a later pass.
- 2026-08-14 09:58 IST | `eslint.config.js` | Merging the QA agent's `.claude/skills` eslint-ignore fix exposed a second, bigger instance of the same problem: running a repo-root `npx eslint .` with 5 other background agents' worktrees active reported 4005 problems, not the 34 the QA agent had verified | Success | Root cause: `.claude/worktrees/` (git worktree checkouts for parallel background agents, each one a full copy of `src/`) was never excluded from the eslint glob either — same class of bug as `.claude/skills`, just not visible when the QA agent verified its own fix (no other worktrees existed yet at that moment). Added `.claude/worktrees` to the same ignores array. Re-ran `npx eslint .`: back down to 34 problems, confirmed via `git diff origin/main..HEAD` that all 34 sit in 3 files this branch has never touched (`blog.functions.ts`, `blog.server.ts`, `schema.tsx`) — genuine pre-existing debt, out of scope. `tsc --noEmit` clean, `npm run build` succeeds.
- 2026-08-14 (Agent 4, MULTI_AGENT_WORKFLOW.md) | `src/routes/index.tsx`, `src/routes/pricing.tsx` | Brand-voice quality pass (Section 11.1) on the homepage and pricing page, per this round's 6-agent split — a QUALITY PASS on the already founder-approved copy, not a rewrite | Success, mostly "already good, left alone" | Read the whole approved Step 4 draft (PROJECT_BRIEF.md) and PENDING.md's history first. Repo-wide grep confirmed zero "Bharat Business Ideas"/"Best Business Ideas" occurrences outside historical log lines (standing item from 2026-08-08 22:20 IST stays closed) and zero real emoji anywhere in either file (only a plain "✕" close glyph and "→"/"⌕" typographic symbols, not emoji). Grepped both files for generic-SaaS buzzwords ("seamless", "unlock your potential", "empower", "leverage", "best-in-class", etc.) and mechanism-leak language ("prompt", "paste", "q=") — zero hits; the existing "Claude or Perplexity" mentions all match the already-established exception (naming supported platforms, never explaining the handoff). Found and fixed two real issues, both left untouched by the Step 4 draft: (1) `pricing.tsx`'s "Why validation is free" section read like dry SaaS-explainer prose ("calling the same public AI APIs anyone can access directly") instead of the short-punchy-line voice used everywhere else on the site — rewritten to match the established rhythm without changing the underlying claim or leaking the mechanism. (2) `index.tsx`'s `DynamicActivityToast` (the rotating "Founder from Austin, US..." notification) had two literally false claims — "unlocked Lifetime Access" — while checkout is confirmed not live (`pricing.tsx`'s own disabled "Checkout not live yet" button); this is the same class of problem as the fabricated "767 founders / WhatsApp group" stat and "11 people, 11 states" team copy already caught and fixed earlier this session, just not caught in this specific component before. Reworded both to "checked out Lifetime Access pricing" (true right now, still reads as real activity). Same component also hardcoded a stale domain, `bestbusinessideas.net` — a leftover from the rejected "Best Business Ideas" name era, inconsistent with the real `businessidea.io` used everywhere else (`site-config.ts`) — fixed to match. Everything else read strong and matched the approved voice already (hero, both hero panels, who-we-are banner, problem-we-found, research-standard, why-this-exists, pricing philosophy, comparison section, promise section, team/who-for/inspired-by sections) — deliberately left untouched per the "don't manufacture changes" instruction. **Verified**: `npx tsc --noEmit` clean, `npx eslint src/routes/index.tsx src/routes/pricing.tsx` clean (one prettier auto-fix on my own new paragraph wrapping), `npm run build` succeeds. Ran the local dev server with the documented `BBI_LOCAL_MOCK=1` Supabase-mock pattern (temporary, added to `ideas.functions.ts`, then fully reverted via `git checkout` immediately after — confirmed absent from the working tree before committing, along with the build-regenerated `routeTree.gen.ts`) plus headless Chromium via Playwright; screenshotted the homepage and pricing page at desktop (1440px) and mobile (390px), both top-of-page and full-page, and actually looked at all of them — hero, pricing cards, the "Why validation is free" rewrite, and the corrected toast/domain all render cleanly with no overlap, no broken layout, no emoji visible anywhere. Did not touch `MULTI_AGENT_WORKFLOW.md`'s other 5 agents' files (`site-shell.tsx`, `idea.$slug.tsx`, browse/category routes, `eslint.config.js`), payment/Razorpay code, or Gemini/n8n pipeline code, per this round's constraints. One process note: this worktree's branch (`worktree-agent-a2e8aa220c8c9be86`) was found 22 commits behind the stated base branch `claude/bbi-continuation-sj6nbr` and carrying 3 unrelated stray commits ("Lovable update", "Work in progress") not on that base — `git checkout -B` was used to point this branch at the real base before starting any work, since the stray commits predated this round and weren't part of this task.
- 2026-08-14 10:06 IST | `src/components/site-shell.tsx` (`MobileMenu`, mobile-menu section only) | Resuming the mobile/tablet responsive audit (Build Order step 7, Section 12.9): confirmed real bug — the header's 4th desktop dropdown, "Browse by type" (`STATIC_GROUPS`, 10 links across "By Who You Are"/"By Investment"), was never ported into `MobileMenu`, which only replicated Categories/Explore/Company. Below the `lg` breakpoint (desktop nav is `lg:hidden`, per the earlier PR #7 breakpoint fix) there was literally no way to reach those 10 links at all — not in the collapsed header, not in the slide-out menu — a real navigation gap on both mobile (390px) and tablet (768–1023px), not a cosmetic squeeze | Success | Added a "Browse by type" section to `MobileMenu` mirroring the desktop panel's two sub-groups (`By Who You Are`, `By Investment`), rendered as full-width plain `<Link>` rows (matching the existing Explore/Company pattern already in this menu, not `CategoryBadge` pills — full-width rows are the more touch-friendly, truncation-safe choice here, see next entry for why pills specifically are the riskier pattern) linking to `/browse` same as the desktop version. Needed a `Fragment` import (grouped title+items per sub-group without an extra wrapper `<div>` disturbing the `nav`'s `grid gap-1` rhythm). Verified with the established local-mock (`BBI_LOCAL_MOCK=1`) + Playwright pattern on my own dev server (port 5311, isolated from sibling agents' worktree servers) at 390px and 768px: screenshot confirms all 10 links render with full untruncated text, no horizontal overflow (`document.documentElement.scrollWidth <= innerWidth` checked programmatically), section reachable at both widths. `tsc --noEmit`/`eslint <touched file>`/`npm run build` all clean. Reverted the `BBI_LOCAL_MOCK` patch on `ideas.functions.ts`/`blog.server.ts`/`local-mock-data.ts` and `routeTree.gen.ts` (the latter regenerates on every `dev`/`build` run, so reverted twice — once before this check, once again after `npm run build` regenerated it) before committing; `git status` confirms only `site-shell.tsx` is staged.
- 2026-08-14 10:06 IST | none (investigated, not fixed — `src/components/category-badge.tsx` deliberately left untouched) | Investigated the hypothesis that `CategoryBadge`'s inner `<span className="truncate">` reproduces the earlier silent-clip bug (the one fixed in `.iv-tag-cloud .iv-tag`, see the 2026-08-09 21:43 entry) — it's structurally the same precondition: the span is a direct flex-item child of `.glass-pill`, which sets `justify-content: center`, and that combination is exactly what made `text-overflow: ellipsis` fail to render last time | Ruled out as originally hypothesized; a different, smaller, real issue found instead — not fixed this round | Checked every real `CategoryBadge` call site with Playwright + real screenshots (not just computed-style reads): mobile Golden Tree pills at 390px (short hardcoded labels, no clipping), footer "Popular categories"/Platform/Company/Legal columns at 768px (short labels, no clipping), and the desktop "Browse by type" dropdown panel at 1280px using the genuinely long `STATIC_GROUPS` labels (the one place on the site long enough to stress-test this). Result: 2 of the 10 pills there ("Zero Investment Business Ideas", "Work From Home Business Ideas") do get their label shortened — but a real "…" ellipsis glyph **does** render correctly (confirmed with a cropped 3x-scale screenshot of each pill), which is the opposite of the original bug's signature (no ellipsis shown at all, characters silently cut from both ends). So the specific hypothesis — reproducing the *silent, no-indicator* clipping — is ruled out; the inner span's ellipsis mechanism works here. What's real instead is a narrower, lower-severity issue: those 2 pills' fixed-ish capsule width is a little too tight for their label at that one breakpoint, so the visible text is legitimately shortened (with clear affordance) rather than invisibly. Per this round's scope ("fix only if confirmed real" — meaning the specific hypothesis), and since this is a materially different and smaller problem than what was asked about, not touching `category-badge.tsx` this round. Flagging honestly for whoever picks this up next: a real fix would mean either loosening `min-width`/padding on those two specific `STATIC_GROUPS` labels, or (bigger, riskier lift, needs its own round) making the component itself let long labels wrap onto a second line the way `.iv-tag-cloud .iv-tag` now does — but that requires touching the inner span's own `white-space`/`overflow`, which the current CSS-only fix never reaches, and would need re-verifying every call site (header dropdowns, footer, Golden Tree), not just the two affected pills. First-run screenshots at 1280px also showed apparent ~15-20px clipping across nearly every pill on the page, including ones with plenty of room — traced that to test-script contamination (the query wasn't scoped to the open dropdown panel and picked up other pill instances, some mid-hover/transform), not a real defect; re-ran scoped strictly to the open panel's own pills before trusting any number, which is the actual source for the 2-of-10 figure above.
- 2026-08-14 09:07 IST (Agent 5, multi-agent brand-voice round) | `src/routes/browse.tsx`, `src/routes/category.$categorySlug.index.tsx`, `src/routes/category.$categorySlug.$subcategorySlug.tsx` | Section 11.1 voice audit of the core browsing flow (headings, intro/description text, empty-state copy, breadcrumbs, CTA copy) | Success, small scope — most of this UI was already fine | Read every visible string in the 3 files. Most of it was already right and left untouched: the `H1`s ("The full idea library", plus the data-driven category/subcategory names), the `{N} ideas · {N} subcategories · {N} categories` count lines, breadcrumb labels, and the `bbi-heading-glow`/`.iv-plainlinks` category-heading treatment (both from this session's earlier fixes — confirmed still present, did not touch either) are functional/plain by design and already match the "less prose than the homepage" guidance — no invented copy added. Zero emojis confirmed (grepped for the emoji Unicode ranges across all 3 files, none found) and no mechanism-leak language ("Claude"/"Perplexity"/"paste"/"prompt" all absent from these 3 files — that risk lives on `idea.$slug.tsx`, owned by a different agent this round, not touched). The one real gap: all 6 `errorComponent`/`notFoundComponent` strings (2 per file — these ARE the empty-state UI; a category/subcategory with zero ideas can't have a valid slug to reach in the first place since `categoryName`/`subcategoryName` are derived from `ideas[0]`, so the loader's own `throw notFound()` already handles that case, landing on these exact strings) read as bare placeholder fragments ("Not found.", "No such category.", "No such subcategory.", "This category could not be loaded.", etc.) — generic and a little cold next to the rest of the site's voice. Tightened all 6 to short, plain, slightly warmer sentences ("That page doesn't exist.", "We don't have that category.", "We don't have that subcategory.", "Couldn't load the idea library — try refreshing.", "Couldn't load this category — try refreshing.", "Couldn't load this subcategory — try refreshing.") — still one line, still functional error-page tone (not marketing copy), just not bare fragments. Did not add navigation links back to `/browse` on these screens (none existed before) since that's a structural/UX addition, not a copy tighten, and out of this pass's stated scope. **Verified with the established `BBI_LOCAL_MOCK=1` pattern**: temporarily patched `src/lib/ideas.functions.ts`'s `db()` to return a chainable mock (3 categories/4 subcategories of realistic mock ideas, including one long category name to confirm the existing wrap/heading-glow fixes still hold and one deliberately idea-less category slug to exercise the notFound path) and `vite.config.ts` to bind `127.0.0.1` (this sandbox has no IPv6, `vite dev`'s default `host: "::"` fails with `EAFNOSUPPORT` otherwise) — ran the real dev server, screenshotted all 3 routes plus both notFound states at desktop (1440px) and mobile (390px) with Playwright before and after the copy change, confirmed the before/after diff is exactly the 6 strings and nothing else moved or broke. Both temp files reverted via `git checkout` before commit (confirmed via `git status`, and `src/routeTree.gen.ts` reverted a second time after `npm run build` regenerated it). `tsc --noEmit` clean, `eslint` clean on all 3 touched files (2 auto-fixed prettier line-wrap nits from the longer sentences, no content change from the fix), `npm run build` succeeds.
- 2026-08-14 (round 2, Agent 6) IST | `src/components/site-shell.tsx` (footer copy only), `src/routes/blog.index.tsx`, `src/routes/blog.$slug.tsx`, `src/routes/about.tsx`, `src/routes/contact.tsx`, `src/routes/terms.tsx`, `src/routes/privacy.tsx`, `src/routes/gdpr.tsx`, `src/routes/refund-policy.tsx` | Brand-voice audit of footer/blog/legal copy per Section 11.1, scoped strictly to my file list (never touched the header/mobile-menu section of `site-shell.tsx` — that's Agent 2's file this round) | Success | **Footer** (`site-shell.tsx`): left the approved tagline, column headings (Platform/Company/Legal), link labels, and the product blurb alone — all already strong. Tightened one weak line: "Idea data served live from the BBI database." (dev-note phrasing, exposes the word "database" to visitors) → "Every idea here is live — updated in real time, never stale." Same honest claim, no implementation talk. **Blog** (`blog.index.tsx`, `blog.$slug.tsx`): the intro copy and both error/not-found states leaked backend implementation details to visitors — "published from our editorial CMS, rendered in BBI's own design system... stay separate systems" (meta description + on-page intro), "The connected WordPress instance did not respond" (index error state), "does not exist on the connected blog" (detail not-found state). None of that belongs in front-of-site copy; rewrote all three to state the same facts (real long-form writing, distinct from the idea library; nothing faked; article's gone) without naming the CMS/stack. Left the plain "No posts published yet." empty state as-is — already fine. **About** (`about.tsx`): found a real mechanism-leak violation of the founder's own standing rule (PENDING.md 2026-08-09 02:40: never say "we hand you a prompt" / name the destination AI account in marketing copy) — the "Where AI fits" section still said "hands you a fully researched prompt on your own Claude or Perplexity account, free," the exact phrasing already fixed everywhere else on the homepage. Rewrote to match the established replacement pattern: "...gets you real research — [same 5 specifics] — free, using AI tools you already pay for. It runs on your account, not ours." Also tightened "How ideas are organised" (removed "read live from our idea database" / "hardcoded in the interface" engineering jargon, kept the same live-not-stale claim) and de-jargoned one "What we do not do" bullet ("the blog is a separate system" → "the blog and the idea library are kept apart, on purpose"). **Contact copy** (`contact.tsx`) was already strong — no rewrite, only the email domain fix below. **Legal pages** (`terms.tsx`, `privacy.tsx`, `gdpr.tsx`, `refund-policy.tsx`, and `disclaimer.tsx` which needed no change at all): left every substantive sentence untouched per the instruction not to add voice to legal copy — these already read accurate and unembellished. **Real bug found and fixed across all 5 contact/legal files**: every mailto link and displayed email address still used the deprecated `@ideavault.ai` domain (the old "IdeaVault AI" brand name PROJECT_BRIEF.md Step 4 says was supposed to be replaced site-wide) — `hello@`, `research@`, `privacy@`, `security@` all switched to `@businessidea.io`, matching the confirmed domain used everywhere else (footer tagline, `README.md`, `PROJECT_BRIEF.md`). Repo-wide grep confirms zero `ideavault.ai` occurrences left in `src/` (the two remaining `IDEAVAULT_*` hits are Supabase env-var names in `ideas.functions.ts`/`auth-client.ts` — internal config, not visible copy, correctly out of scope and untouched). **Disclosed, not verified**: businessidea.io's mail isn't confirmed live yet — the custom domain itself is intentionally still disconnected (Build Order Step 17) — so these addresses are the *correct* brand-consistent ones but founder should confirm real inboxes exist there before launch; keeping the old `ideavault.ai` addresses live would have been worse (stale deprecated brand name, publicly exposed). **Not touched, flagged for a future pass**: `services.tsx` has the same "hands you a fully researched validation prompt into your own Claude or Perplexity account" mechanism-leak phrasing as the `about.tsx` bug fixed here, but that file isn't in this agent's file list this round — left alone to respect merge-order file boundaries, not forgotten. Zero emojis confirmed across every file touched (only `→`/`✕` typographic glyphs, not emoji). **Verified locally**: `BBI_LOCAL_MOCK=1` pattern (temporary Proxy-based mock on `db()` in `ideas.functions.ts` + a matching mock on `wpFetch` in `blog.server.ts` for the blog's WordPress calls, both guarded behind the env var), local dev server, Playwright screenshots of the footer (desktop + mobile), about, contact, and blog-index pages confirming the new copy renders correctly with no layout regressions — all reviewed with the Read tool, not just captured. Both mock patches and `routeTree.gen.ts` reverted via `git checkout` before commit (confirmed via `git status`, twice — once after `npm run build` regenerated `routeTree.gen.ts` again, reverted a second time). `tsc --noEmit` clean, `eslint` clean on every touched file (2 trivial prettier formatting nits auto-fixed, no content change), `npm run build` succeeds. One process note: this worktree's branch had been forked from a stale point (predating this round's `MULTI_AGENT_WORKFLOW.md` commit and the approved footer tagline itself, which was missing entirely from the initial checkout) — fixed at the start of this task by resetting the worktree branch onto `claude/bbi-continuation-sj6nbr`'s actual tip before making any edits, confirmed safe since the 2 commits that would have been dropped only touched `routeTree.gen.ts` (a generated file, always reverted anyway).
- 2026-08-15 (Round 2, Agent 3 "Pill Truncation", see `MULTI_AGENT_WORKFLOW.md`) | `src/components/category-badge.tsx`, `src/styles.css` | Resuming the 2026-08-14 10:06 IST investigation: made `CategoryBadge` labels WRAP onto a second line instead of truncating, closing the gap that entry flagged but deliberately left open (2 of the 10 "Browse by type" pills — "Zero Investment Business Ideas", "Work From Home Business Ideas" — had real label text hidden behind a working ellipsis) | Success, one real regression found and fixed in the same pass (see below) | **The fix**: removed `truncate` from both the inner label `<span>` and the outer `.glass-pill` link in `category-badge.tsx`; the label span now gets `whitespace-normal break-words leading-snug` and the outer link gets a `category-badge` marker class + `text-center` (so wrapped lines stay centered, matching `.iv-tag-cloud .iv-tag`'s existing `text-align: center`). Baked the wrap behavior into the component itself rather than depending on callers opting in via the `iv-tag` className (which every current call site happens to pass, but a future one might not) — this also covers the Golden Tree's standalone pills, which never sit inside an `.iv-tag-cloud` wrapper. Added `.category-badge.cb-sm`/`.cb-md` min-heights and a bit more vertical padding (`py-0.5`→`py-1`, `py-1.5`→`py-2`) in `styles.css` so a 2-line pill has breathing room instead of looking cramped; short single-line pills are only fractionally taller than before, not a visible regression. **Real regression found and fixed before committing**: the Golden Tree's absolutely-positioned nodes (`.bbi-tree-node-m`/`.bbi-tree-node-d`, both `position: absolute` with only `left` set, no `right`) turned out to depend on the old `nowrap` behavior in a way I hadn't anticipated — an `auto`-width absolutely-positioned box's shrink-to-fit sizing is bounded by "containing-block width minus its `left` offset," so a node placed toward the right edge (e.g. the mobile tree's "Work From Home" node at `x:70%`) has very little of that computed budget. `white-space: nowrap` had been accidentally protecting these nodes from the squeeze (nowrap forces min-content to equal max-content, so shrink-to-fit always resolved to the label's true full-width regardless of the constrained budget); once the label became wrappable, min-content dropped to just its widest single word, so the squeeze had room to actually apply — caught this via an actual mobile (390px) screenshot showing "Work From Home" and "Side Hustle" force-wrapped one word per line, an objectively worse result than the truncation this round set out to fix. Fixed by adding `width: max-content` to both `.bbi-tree-node-m` and `.bbi-tree-node-d` in `styles.css`, opting these standalone nodes out of the shrink-to-fit squeeze entirely (their labels are short by design and were never meant to wrap in the first place, unlike the header/footer pills which sit in normal flex flow and don't have this absolute-position quirk) — re-screenshotted after, confirmed fixed at both breakpoints, desktop unaffected. **Verified**: `npx tsc --noEmit` clean, `npx eslint src/components/category-badge.tsx` clean (2 trivial prettier auto-fixes, no content change), `npm run build` succeeds. Used the established `BBI_LOCAL_MOCK=1` + local dev server + Playwright pattern (temporary chainable-Proxy mock on `ideas.functions.ts`'s `db()`, 4 mock categories/ideas including the 3 real `FEATURED_IDEA_IDS` so the homepage loader resolves cleanly) and actually looked at every screenshot, not just asserted pass/fail: desktop (1440px) "Browse by type" dropdown open (both full-panel and a cropped shot) — both long labels now show in full across 2 lines, all 8 short labels in the same panel stay single-line, unchanged; desktop footer (5 columns) — no regression, all pills single-line (labels are short); desktop Golden Tree — unaffected, single-line, correctly centered; mobile (390px) menu top and its "Browse by type" section — this section renders as plain full-width `<Link>` rows per the 2026-08-14 10:06 IST fix, not `CategoryBadge` pills, so correctly untouched by this change, confirmed still fine; mobile footer — no regression; mobile Golden Tree — the regression above, caught and fixed, re-verified clean. One process/tooling note for whoever verifies next in this sandbox: `vite dev`'s React tree runs in StrictMode, which double-invokes functional `setState` updaters (`setOpen(v => !v)`) on a single click — for a boolean toggle this nets back to the original value, making the "Browse by type" dropdown trigger look unresponsive to `.click()` in Playwright even though the click genuinely fires (confirmed via a `MutationObserver` on `aria-expanded`, which showed `true` then immediately `false` from one click). Not a real app bug — production builds don't double-invoke — but `hover()` (which calls `setOpen(true)` directly, not a functional updater, so idempotent) is the reliable way to open these dropdowns under `vite dev` + StrictMode. Reverted the `BBI_LOCAL_MOCK` patch on `ideas.functions.ts` and the build/dev-regenerated `routeTree.gen.ts` before committing (`git checkout --`, confirmed via `git status` that only `category-badge.tsx` and `styles.css` are staged). Did not touch `site-shell.tsx`, `index.tsx`, or any file outside my exclusive pair this round; no payment/Razorpay, Gemini/n8n, or cursor code touched.
- 2026-08-14 (Agent 1 of the 6-agent parallel round, see `MULTI_AGENT_WORKFLOW.md`) | `src/components/validate-button.tsx`, `src/components/validate-context-input.tsx` (new), `src/lib/validate-shared.ts`, `src/lib/validate.functions.ts`, `src/lib/validate.server.ts` | Founder (direct voice instruction): redesign the Validate flow — no Gemini for now, show two real-logo buttons (Claude, Perplexity) instead of the old "Validate for free → pick a platform" two-step, and add one more feature below them: an optional textbox for the user's own words/context that gets folded into the master prompt | Success, one deviation from the historical spec disclosed below | **UI**: `ValidateButton` now shows both platform buttons immediately (no intermediate reveal step) — `PlatformButton` reuses the exact `Spotlight` + `usePillInteraction` + `.glass-pill` pattern already established by `site-shell.tsx`'s `FooterCta`/`index.tsx`'s `HeroCta`, not a new visual language. Real brand marks via `react-icons/si`, same import pattern as `site-shell.tsx`'s "Built With" section (`SiClaude`, and — unlike that section's ChatGPT/Grok gap — `SiPerplexity` does exist in the installed `react-icons` v5.7 set, confirmed by reading its type defs and rendering its actual SVG path before relying on it, so **both** buttons show a genuine logo; no text-only fallback was needed here). Below the buttons: new `ValidateContextInput` component, a `glass`-panelled optional `<textarea>` (600-char cap, live counter, focus ring in `--accent`) with placeholder copy written for this task: "Sharpen it with specifics: your city or target market, the budget you're starting with, skills or an edge you already have, or one angle you want stress-tested hardest." A caption under it states plainly what happens to the text ("folded straight into your prompt... nothing you type here is saved on our side") since this screen is a functional control the user is already using, not marketing copy — per the task brief, PROJECT_BRIEF.md Section 8's mechanism-hiding rule governs marketing copy elsewhere, not this picker. **Plumbing**: `context` is optional end-to-end — `ValidateContextInput` → `ValidateButton`'s mutation → `getValidateUrl` (new optional `context` field, `z.string().trim().max(VALIDATE_CONTEXT_MAX_LENGTH)`) → `buildValidateUrl`/`buildValidationPrompt` (`validate.server.ts`), which appends it as its own labeled section ("The user added this context themselves before sending — it's real signal, not filler...") between the idea facts and the "Structure the report" instructions, only when non-empty after trimming — zero input reproduces the exact prior prompt byte-for-byte. `VALIDATE_CONTEXT_MAX_LENGTH` (600) lives once in `validate-shared.ts` so client counter and server validator can't drift. Step 0 auth/plan precondition (PROJECT_BRIEF.md Section 3.2) is unchanged — anonymous still routes to `/sign-in`, authenticated-without-a-plan still opens `PaywallPopup`, both gate BEFORE either button's mutation fires. **Deliberate, disclosed deviation from PROJECT_BRIEF.md Section 8's literal "never build... a visible prompt textbox"**: that line is about the server-built master prompt staying invisible/uncopyable, which it still is — nobody can read or copy it anywhere in this UI. The new box only holds words the user typed themselves, appended server-side; founder's direct instruction this round explicitly asked for it, so treating that as authorizing this one addition rather than a conflict. **Verified**: `npx tsc --noEmit` and `npx eslint` clean on every touched/new file; `npm run build` succeeds (`idea._slug` and `react-icons` remain their own code-split chunks, consistent with the "Built With" section's existing pattern — no new libraries added to the main bundle). Local browser verification per the session's established `BBI_LOCAL_MOCK=1` workflow: patched `ideas.functions.ts`'s `db()` with a mock idea row (temporary, reverted via `git checkout --` before commit, confirmed absent from the diff) and ran `vite dev` under Playwright/headless Chromium (`/opt/pw-browsers`). Screenshotted and read (not just asserted) both states — desktop and mobile (390px) — showing the two branded buttons with real rendered logos and the optional box with its placeholder; typed real text into the box, confirmed it visibly appears and the character counter updates. Also temporarily hardcoded `useAuth`'s return value (same revert discipline) to drive all three gated states without real Supabase credentials: authenticated-with-plan (clicking Claude opened `window.open` — intercepted in the test, not a real new tab — with a `claude.ai/new?q=...` URL whose decoded query contained the idea's real facts **and** the exact typed context text in its own section; clicking Perplexity produced the equivalent `perplexity.ai/search?q=...` URL with the same prompt), anonymous (click routed to `/sign-in?redirect=%2Fidea%2Fmock-idea`, screenshot-confirmed), and authenticated-without-a-plan (click opened the existing `PaywallPopup`, screenshot-confirmed, unchanged copy). Zero emojis anywhere in the new copy (grepped). No Gemini button, code, or reference added anywhere — confirmed by re-reading every touched file. **Test-harness noise, not a shipped bug**: while testing, the footer's "Popular categories" list occasionally hydration-mismatched (SSR rendered a real `CategoryBadge` link, client briefly rendered a plain span) — traced this to the local mock's single-fake-category catalog racing the real `catalogQuery` prefetch/hydrate cycle, not to anything in the 5 files actually touched here (`site-shell.tsx`, `category-badge.tsx`, and real Supabase data were never part of this change); it self-resolved a beat after page load and never affected the Validate section itself, which rendered byte-identical between server and client in every capture. Only touched `idea.$slug.tsx`'s existing `ValidateButton` embed indirectly (no edits needed to `idea.$slug.tsx` itself — the anchor/id wiring it already had was sufficient) plus the listed component/lib files; did not touch `site-shell.tsx`, `index.tsx`, `browse.tsx`, any category route, `pricing.tsx`, or `eslint.config.js`, per this round's file-ownership split.
- 2026-08-14 (round 2, Agent 2 — Copy Consistency, see `MULTI_AGENT_WORKFLOW.md`) | `src/routes/services.tsx` | Follow-up to the 2026-08-14 round-2 Agent 6 entry above, which fixed a mechanism-leak on `about.tsx` and explicitly flagged that `services.tsx` had the same violation, left for a later pass — this is that pass | Success | **The fix**: the "Free validation handoff" card's body copy read "On every idea page, a one-click handoff pre-fills a fully researched validation prompt into your own Claude or Perplexity account. We don't charge for AI analysis — you run it yourself, free." — a direct violation of the standing rule (PROJECT_BRIEF.md Section 11.1 / PENDING.md 2026-08-09 02:40: marketing copy never explains the AI-validation mechanism or names the destination account). Rewrote to the same established replacement pattern used everywhere else on the site: "On every idea page, a one-click handoff gets you a fully researched validation report — free, using AI tools you already pay for." Same honest claim (free, one-click, real research), no mechanism leak. Left the card's bullet point "Runs on your own AI account, not ours" untouched — it already matches the approved `about.tsx` phrasing ("It runs on your account, not ours") and isn't a leak. **Also verified, per task scope, and found clean (no changes needed)**: zero emojis anywhere in the file (grepped Unicode emoji ranges, only match was the literal string `@tanstack/react-router` import, not an emoji); brand name is "BBI" throughout, zero "Bharat"/"Best Business Ideas"/"IdeaVault" occurrences; no email/domain strings in this file at all (nothing to fix there — the `@ideavault.ai` bug from the prior entry was in contact/legal files, not this one). Rest of the file's copy (service names, other 3 cards, "Where to start" section) was already fine and deliberately left untouched — targeted fix only, not a rewrite. **Process note**: this worktree's branch was found 37 commits behind the stated base `claude/bbi-continuation-sj6nbr` (missing `MULTI_AGENT_WORKFLOW.md`, this file's round-2 entries, and all of round 1/2's work entirely) — fast-forwarded onto the base's real tip before starting any work, confirmed safe since the local branch had zero commits of its own beyond that shared ancestor (nothing to lose). **Verified**: `npx tsc --noEmit` clean, `npx eslint src/routes/services.tsx` clean, `npm run build` succeeds. Local browser check per the established `BBI_LOCAL_MOCK=1` pattern: temporarily patched `ideas.functions.ts`'s `db()` with a chainable Proxy mock (guarded behind the env var), ran `vite dev` on an isolated port, and used Playwright (headless Chromium at `/opt/node22/lib/node_modules/playwright`) to screenshot the `/services` route at desktop (1440px) and mobile (390px) — both reviewed with the Read tool, not just asserted: new copy renders correctly, no layout regression, no truncation, no emoji visible. Mock patch and `routeTree.gen.ts` both reverted via `git checkout --` before commit (confirmed via `git status` — only `services.tsx` staged). Did not touch `about.tsx`, `site-shell.tsx`, payment/Razorpay code, or Gemini/n8n pipeline code, per this round's file-ownership split.
- 2026-08-15 (round 2, Agent 4) | `public/llms.txt` (new file only) | Built BBI's own `llms.txt` per PROJECT_BRIEF.md Section 11 / Build Order step 16, "using real numbers only" | Success, one disclosed limitation on how "real" is defined | **Numbers actually verified, not estimated or copied from UI copy**: ran `execute_sql` directly against the live Supabase project (`jqzadwobnfypmytcbpkw`, the active one — confirmed via `list_projects`, not the other `INACTIVE` project in the same org) at write time: `280` published ideas (`status='completed'`) across `13` categories, plus the exact per-category breakdown (Zero Investment 50, Work From Home 50, Side Hustle 50, Passive Income 31, Low Investment 25, Creator & Media 10, AI & Automation 10, FinTech & Finance 10, Health & Fitness 10, Education & EdTech 10, Tech & SaaS 10, E-Commerce & Retail 10, Productivity & Workflow 4 — sums to 280). Pricing (₹199/3 months, ₹399/lifetime) pulled from `pricing.tsx`'s actual rendered `plan.price` values, not memory. **What is honestly a snapshot, not a live figure**: `public/llms.txt` is a static file with no server logic (matches the task's own file-ownership constraint — new files only, no `src/` route/loader), so the 280/13 numbers are frozen at today's date and will drift as the n8n/Gemini pipeline keeps adding ideas (PROJECT_BRIEF.md Section 4.1 says the catalog is scaling toward 1,000+ within days). Disclosed this explicitly inside the file itself ("a live, growing dataset... treat these as a snapshot... will be higher by the time you read this") rather than presenting a static number as permanently current — this is the same honesty standard Section 11 asks for on the accuracy-percentage/testimonial front, applied to the count itself. Also checked and confirmed real (not invented): all 13 category slugs used for `/category/{slug}` links (queried alongside the counts, not guessed from the display name), every top-level route linked (`/`, `/browse`, `/pricing`, `/about`, `/services`, `/blog`, `/contact`, `/sign-in`, `/terms`, `/privacy`, `/disclaimer`, `/refund-policy`, `/gdpr`) exists in `src/routes/`, and `/search` is listed under its own "Optional" heading with a note that it's `noindex,follow` and not meant to be indexed as individual query pages, matching the existing route-level convention. **Mechanism-leak check**: described the Validate action as "free structured research using AI tools you already have access to, on your own account" — no "prompt," no "paste into Claude," no platform-picker mechanics — consistent with the standing rule (PENDING.md 2026-08-09 02:40 and every brand-voice pass since). Brand name "BBI — Bro Business Ideas" used as the H1 (repo-wide grep for "Bharat"/"Best Business Ideas" in `public/llms.txt` returns nothing); domain used throughout is `https://businessidea.io` (the confirmed final domain per this task's own instructions), not the current Lovable preview domain the site still actually resolves to pre-Step-17 — flagging this so it isn't mistaken for a live-tested URL; the file's own *serving mechanism* was verified against the real dev server (see below), just not against a not-yet-connected custom domain. Zero emojis (grepped). **Serving verified two ways, not just built**: (1) `npm run build` — confirmed `public/llms.txt` lands byte-identical at `.output/public/llms.txt`, alongside `favicon.ico`/`robots.txt` (same static-asset path, nothing custom needed); (2) ran `vite dev` on an isolated port (5391) and `curl`'d `http://127.0.0.1:5391/llms.txt` directly — `HTTP_STATUS:200`, `Content-Type: text/plain`, response body byte-identical (`diff`, 5238 bytes both sides) to the committed source file, and cross-checked `/robots.txt` on the same server returned the identical status/content-type pattern as further confirmation this is genuinely the standard `public/` static-serving path, not a fluke. Dev server killed after the check. `npx tsc --noEmit` clean, `npm run build` clean (only touched file is new, no `src/` edits at all — nothing for `eslint` to lint on a `.txt` file, confirmed it's correctly ignored rather than silently misconfigured). `src/routeTree.gen.ts` was regenerated by both the build and the dev-server run and reverted via `git checkout --` each time (checked via `git status --porcelain` after each, confirmed absent before commit). No `BBI_LOCAL_MOCK` patch was needed or used this round — this task never touched any Supabase-backed loader, only queried the live DB read-only via the Supabase MCP for the count itself. **Not touched**: no `src/` route, component, or config file — per this task's explicit file-ownership constraint, real numbers were pulled via direct SQL rather than by editing `ideas.functions.ts`/`getCatalog` to expose them, so nothing needed wiring for `llms.txt` itself to render correctly as a static asset.
- 2026-08-15 11:21 IST | `src/components/ambient-scene.tsx` | Two decorative background ring accents (lines ~82, ~86) still carried hardcoded warm-orange `border-[oklch(...)]` values left over from an old palette, while the rest of the site (including `.bbi-twin-ring`'s glow gradients in `src/styles.css`) is indigo/violet | Success | Recolored both to on-brand violet, matched against the actual brand tokens rather than eyeballed: converted `--violet` (`#8886DB`) and `--violet-soft` (`#B6B5E3`) to oklch — `oklch(0.658 0.125 283.5)` for the first ring (was `oklch(0.723 0.161 56)`), `oklch(0.79 0.065 285.8)` for the second, lighter/softer ring (was `oklch(0.805 0.135 65)`) — same lightness-pair relationship as the original (first ring darker/more saturated, second lighter/softer), just shifted from orange hue to violet hue with brand-matched chroma (both notably lower-chroma than the oranges they replaced, per the "soft, not loud" brief). Sizes/positions/blur/opacity/animation all untouched, only the two `border-[oklch(...)]` values changed. Verified live: temporarily patched `db()` in `src/lib/ideas.functions.ts` behind a `BBI_LOCAL_MOCK=1` env check (mock Supabase-shaped client returning one fake completed idea row) since this sandbox's private npm registry 403s on `bun install` — same confirmed-not-transient constraint noted 2026-08-08 19:49 — so used `npm install --legacy-peer-deps` against the public `registry.npmjs.org` instead (already in this sandbox's no-proxy allowlist), which resolved fine. Ran `BBI_LOCAL_MOCK=1 npx vite dev --host 127.0.0.1 --port 5183`, screenshotted the homepage with the `/opt/node22/bin/playwright screenshot` CLI against the preinstalled `/opt/pw-browsers` chromium, and actually looked at the PNG — both rings render as soft indigo/violet blooms, no orange anywhere on the page. Reverted `src/lib/ideas.functions.ts` via `git checkout --` before committing (confirmed via `git status` — only `ambient-scene.tsx` modified); `src/routeTree.gen.ts` was never touched by this pass so there was nothing to revert there. `npx tsc --noEmit`, `npx eslint src/components/ambient-scene.tsx`, and `npm run build` all clean.
- 2026-08-15 11:30 IST | `src/components/idea-card.tsx` | Round 2 Agent 6 — fixed the loading-flash: an already-signed-in visitor no longer sees a flash of the "locked"/"Sign in to view" overlay while `useAuth`'s session check is still in flight | Success | Root cause: `locked` was computed as `auth.status !== "authenticated"`, so the transient `auth.status === "loading"` window (before the async Supabase session check resolves) rendered identically to a confirmed-signed-out visitor — blur, lock icon, "Sign in to view", all of it. Changed the one line to `locked = auth.status === "anonymous"`, so only the definitive signed-out status renders the locked treatment; `"loading"` now renders the same as `"authenticated"` (content visible, unblurred) until the session check resolves one way or the other. `use-auth.ts` untouched — only its existing three `AuthState` statuses (`loading`/`anonymous`/`authenticated`) are read, no signature or shape change, per instruction. Verified two ways: (1) traced `useAuth`'s effect — `session` starts `undefined` (loading), resolves to either `null` (anonymous) or a real `Session` (authenticated) from `getSession()`/`onAuthStateChange`, so `loading` is strictly transient and never a true end state, making it safe to treat as unlocked-neutral rather than a fourth visual state; (2) a temporary local QA harness (throwaway route + temporarily mocked `getCatalog()` server fn, both fully reverted, confirmed via `git status`/`git diff` before commit) rendering `IdeaCard` under Playwright with `useAuth`'s return hardcoded in turn to `loading`, `authenticated`, and `anonymous` — screenshots confirm `loading` and `authenticated` both render clean/unblurred with zero console errors, `anonymous` still renders the full locked overlay exactly as before, so the genuinely-signed-out case is unchanged. Honest limit: this sandbox has no live Supabase reachability (outbound to the project's Supabase host times out — same known constraint noted in earlier entries), so the actual sub-second real-world flash timing was not captured on video/screenshot and can't be — that's inherent to a timing race, not a gap in this fix. The fix is correct by construction (the loading window can no longer render the locked branch at all, full stop) rather than empirically timed. `tsc`/`eslint <file>`/`npm run build` all clean.
- 2026-08-15 06:20 IST | `src/routes/idea.$slug.tsx` | Build Order step 11 / Section 8.2 — auto internal linking added to the idea detail page, max 3 keyword-matched links, using only data already loaded (no new Supabase round-trip) | Success | New "Keep exploring" card in the article body (outside the sign-in blur, after the tags row): links to the idea's own subcategory hub and category hub (guaranteed on-topic), plus a third link only when the idea's own keywords/tags genuinely match a title/summary/tag in the `related` list already fetched for the sidebar/bottom cards — capped at 3, fewer if no third match, never padded with an irrelevant link. Prioritizes hub pages over idea links per the brief's stated priority. Copy is plain wayfinding ("This blueprint sits inside X, part of the wider Y lineup...") — no mechanism leaked, zero emojis, matches the existing glass/card voice. Does not touch or duplicate the pre-existing "More in {category}" related-ideas card grid. Verified with a temporary `BBI_LOCAL_MOCK=1` fixture (this repo snapshot had no such flag yet — added and fully reverted, not a real convention to reuse) wired into `getCatalog`/`getCategoryPage`/`getSubcategoryPage`/`getIdeaBySlug`: ran `vite dev`, drove headless Chromium (Playwright, browser binary already present at `/opt/pw-browsers`) to screenshot the idea page desktop (1440px) and mobile (390px) — both actually viewed, block renders correctly, not blurred, all 3 hrefs (`/category/tech-saas/ai-tools`, `/category/tech-saas`, `/idea/no-code-ai-automation-builder-local-services`) returned 200 against the mock server confirming they resolve to real existing routes. `tsc --noEmit`, `eslint src/routes/idea.$slug.tsx`, and `vite build` all clean. `src/lib/ideas.functions.ts` and `package.json` (playwright devDependency added for the screenshot check) reverted to the original committed state before this commit — `git diff --stat` shows only `idea.$slug.tsx` changed.
- 2026-08-15 IST | `src/components/site-shell.tsx` (BuiltWithSection) | Founder: the Built With logos read "text kind of" / faint, and to stop citing licensing for ChatGPT/Grok — the world uses those marks freely, so just show them | Success | Two changes: (1) All 18 logos now render in their real brand color (React cyan #61DAFB, Tailwind #06B6D4, GSAP green, TanStack red, Node green, Supabase green, Framer blue, Gemini purple, Claude/Claude Code clay #D97757, etc.; the marks that are black in their own brand guidelines — shadcn, Vercel, GitHub, ChatGPT, Grok — use the site's near-black foreground so they stay crisp on the light glass) instead of the previous single flat `text-accent` tone, and bumped from h-5 to h-6 so they read as actual recognizable logos rather than faint one-tone glyphs. (2) Filled the two previously-empty slots (ChatGPT, Grok) with real marks — `RiOpenaiFill` and `RiGrokAiFill` from `react-icons/ri` (Remix Icon), which carries both even though Simple Icons doesn't; removed the monogram-initial text fallback entirely since all 18 now have a real logo. No licensing gate — these are the standard brand marks shipped in an installed, MIT-licensed icon package. Verified with the local `BBI_LOCAL_MOCK=1` dev server + Playwright: all 36 rendered items (18 x2 marquee loop) are real `<svg>` marks, ChatGPT and Grok included, each computed-`color` matching its assigned brand hex; screenshotted the strip desktop + mobile and viewed it — logos are clearly recognizable and colorful, marquee still animates, no layout shift. `tsc --noEmit`/`eslint src/components/site-shell.tsx`/`npm run build` all clean. Only `site-shell.tsx` changed.
- 2026-08-23 IST | none (process decision, logged before touching code) | scrollcraft plugin evaluated end-to-end (`claude plugin marketplace add nateherkai/scroll-craft` + `claude plugin install nateherk-design@nateherk`, full `SKILL.md`/`devices.md`/`taste.md`/`uniqueness.md` read, `doctor.mjs` run) before any page work, per founder instruction to understand the tool fully first | Decision: principles-only, no literal engine | The literal scrollcraft engine builds one standalone hand-authored HTML landing page per "build," copying its own `scrollcraft.js`/`scrollcraft.css` (not React/TanStack), driven by photoreal video/stills generated through a **second paid API** (`KIE_AI_API_KEY`, kie.ai) — `doctor.mjs` confirmed that key is unset and ffmpeg (required for its video pipeline) is missing from this sandbox. This conflicts directly with the founder's same-message instructions ("don't set up a second system", "use our existing Gemini key and n8n pipeline", pull colors only from our real Tailwind tokens) and with the fact that most of BBI's pages are Supabase-backed data views, not one-off marketing pages. Asked the founder to choose; answer: **principles-only, everywhere** — no kie.ai, no new API key, no spend, no standalone engine. What's actually being reused: the skill's *design discipline* — `references/devices.md`'s nine device families (scrub/pin/pan/reveal/kinetic/parallax/count/flow/pointer), `references/taste.md`'s spacing/type/colour/depth/motion rules (all directly compatible with the codebase's existing GSAP+ScrollTrigger stack — `Reveal`, `CardFan`, `CountUp`, `Spotlight`, `src/lib/motion.ts`), and `references/uniqueness.md`'s "page grammar" concept for deliberate per-page-type structural variety plus the 6-dimension fingerprint-gate idea (adapted to BBI's own page types, not other people's client builds). Grammar assignment made and about to be logged in a project-local `scrollcraft/FINGERPRINTS.md` registry (seeded via `node <skill>/scripts/workspace.mjs --ensure`, currently empty): homepage → **Chaptered editorial** (fits its ~20 real, thematically distinct sections far better than a filmic scrub-hero, and forbids the AI-slop tells — scroll-cue arrows, `01/06` counters — the taste floor already bans); idea detail page → **Live surface** (the grammar built for "real markup computing real state off real data," which structurally forbids fake dashboards/fake terminals and is the best possible fit for the flagship template); category/subcategory/browse/search → **Gallery/catalog** (museum-label discipline: real spec labels, no persuasion copy, matches the existing honest-founder voice rule); pricing → **Split stage** (two plans compared, "the comparison is the product"). The 10 static/legal pages and the WordPress-sourced blog are explicitly NOT being forced into bespoke cinematic grammars — legal pages get the taste floor applied to the existing `ContentPage` template, and blog posts (arbitrary future WP HTML we don't author) get typographic-rhythm discipline, not per-post scroll choreography, because a per-post bespoke build can't survive content nobody has written yet. For imagery: the founder will generate all stills/video themselves in their own Gemini Pro account from prompts I write per section (tailored to the real content, not generic), then drop the files in for me to wire in — no placeholder/invented imagery ships in the meantime, and any prompt result that doesn't fit gets flagged back for a regenerate rather than forced into place.
- 2026-08-23 21:15 IST | `src/lib/scroll-devices.ts` (new), `src/styles.css` (append only) | Shared scroll-device engine — the floor every page-type rebuild below consumes, built once per the founder's "same engine, same design floor" instruction | Success, fully verified live | Seven hooks translating scrollcraft's `references/devices.md` device vocabulary onto this repo's existing gsap+ScrollTrigger stack (no new dependency, no literal scrollcraft engine copied — see the 21:00 entry above for why): `usePinProgress` (pin device, writes `--sc-p` 0→1 per frame), `useRail` (pan/rail, measures real `scrollWidth` overflow rather than assuming it — devices.md's own documented failure mode), `useStaggerReveal` (flow+in, fires once via IntersectionObserver), `useTilt`/`useMagnet` (pointer devices, gated to `(hover:hover) and (pointer:fine)`, off under reduced motion), `useKineticLines` (masked per-line type reveal, waits on `document.fonts.ready`), `useRevealWipe` (clip-path wipe, 5 directions). Matching `.sc-*`-scoped CSS added to `styles.css` (ease-out token, elevation steps, edge-light, grain texture, text-over-media scrim shapes, rail/gallery/chapter/split-stage layout utilities) — zero new colors, every value derived from the real `html.light` tokens via `color-mix()`. **Verified live, not just typechecked**: built a throwaway route (`src/routes/sc-smoke-test.tsx`, deleted before this commit) exercising all seven hooks at once, ran it under `BBI_LOCAL_MOCK=1 vite dev` (temporary `db()` mock added to `ideas.functions.ts`, fully reverted — confirmed via `git diff` showing zero change to that file after cleanup) with Playwright driving real pointer/scroll events: kinetic split produced real line spans with intact text (62 chars in, 62 out); tilt applied real `rotateX`/`rotateY` on hover; magnet applied a real `translate` toward the cursor; pin's `--sc-p` measured 0.2222 then 0.6667 across two scroll steps and the stage's `getBoundingClientRect().top` held at ~0 (genuinely pinned, not just progressing); rail measured a real 1136px overflow and translated exactly `-1136px` at full scroll, matching to the pixel; all 3 stagger items reached `.sc-in`. Zero console/page errors from the engine itself. `npx tsc --noEmit`, `npx eslint`, and `npx vite build` all clean after cleanup; `src/routeTree.gen.ts` reverted via `git checkout --` (regenerated twice, by dev server and by build, reverted both times, confirmed via `git status --porcelain`). Nothing in this commit touches an existing page — it is additive infrastructure only, per the butterfly-effect map's HIGH-blast-radius rule for `styles.css` (one owner, this round, done before any page-type work starts).
- 2026-08-23 21:35 IST | `src/routes/idea.$slug.tsx` | Flagship idea template rebuilt under scrollcraft's **Live Surface** grammar (`references/uniqueness.md` §2.3), the first of the per-page-type grammar assignments | Success, verified live desktop + mobile | Grammar chosen because §2.3 is the one built for "real markup computing real state off real data" and structurally *forbids* fake dashboards/terminals — the correct fit for a page whose whole job is rendering researched Supabase fields, and it rules out the filmic scrub-hero that §2.1 warns has become the default skeleton. Changes: (1) **hero is now a status readout, not a marketing banner** — mono `IDEA-00214` id, category/subcategory path, and the idea's own real `trend_score` rendered as a `78 / 100` momentum figure in the shared `.sc-spec-label` schema; the duplicate "Validate for free" pill that used to sit in the hero is **removed**, because §2.3 bans marketing chrome there and the real validate action belongs at the close. (2) **New `ComputedVerdictPanel`** using the shared `usePinProgress` hook from `src/lib/scroll-devices.ts` (span 1.6vh): pros and cons are both present from the start (nothing hidden to manufacture a reveal), and the idea's real `verdict` crossfades in off `--sc-p` as the frame holds. Every word in it is the idea's own `pros_json`/`cons_json`/`verdict` — no invented copy. (3) **Close is now an actual input**, per §2.3's "a magnetic button is the wrong ending for a page that spent its whole length being a tool": the decorative centered "Think this one is yours?" CTA block is gone and `ValidateButton` (real platform buttons + the optional context textarea) *is* the ending. (4) Seven `data-anchor` sections added (top/breakdown/verdict/research/steps/faq/related/validate) so the existing `FloatingDock` — which auto-discovers `[data-anchor]` and previously found none on this page — now works as real app-style navigation, which is what §2.3 wants chrome to be. **Two real bugs found and fixed while verifying, neither cosmetic**: (a) **hydration mismatch on every idea page load** — `router.tsx` builds a fresh empty `QueryClient` on both server and client with no dehydration between them, so the component's `useSuspenseQuery` was genuinely re-invoking `getIdeaBySlug` from the browser on first render; because Section 9's variant/gradient/related picks are `Math.random()`-per-call server-side, the two independent calls returned different results and React discarded and re-rendered the tree (`data-variant="hero-left"` vs `"editorial"` etc. captured verbatim in the Playwright output). Fixed by reading `Route.useLoaderData()` instead, which TanStack Router serializes once from the single server-computed loader result; `useSuspenseQuery` import dropped. Confirmed fixed: the variant/gradient diff is absent from the post-fix run. (b) While confirming (a), found a **separate pre-existing sitewide hydration bug** in `src/components/site-shell.tsx:724` (footer "Popular categories" renders `Loading…` server-side, real `<a>` links client-side). Proved it is **not** caused by this work by testing untouched routes: `/about` reproduces it identically, `/browse` does not (its own loader warms the `["catalog"]` cache before render). **Deliberately not fixed in this commit** — `site-shell.tsx` is HIGH blast radius per the butterfly-effect map and renders on every page, so it gets its own isolated round rather than being bundled into a page rebuild. Logged here as an open item. **Verification**: temporary `BBI_LOCAL_MOCK=1` `db()` mock returning one fully-enriched idea row (all v2 pipeline fields populated, 7 FAQs, real pros/cons/verdict) — fully reverted, `git status` confirms `ideas.functions.ts` unchanged. Playwright at 1440x1000 and 390x900: hero momentum readout renders the real `78 / 100` on both; verdict panel confirmed genuinely pinning with live `--sc-p` (0.1225 desktop / 0.1361 mobile); validate close section renders at both widths; **zero horizontal overflow at either width**; screenshots reviewed by eye (not just captured) — auth-gated blur renders correctly for the signed-out case, mobile close reads cleanly. `npx tsc --noEmit`, `npx eslint`, `npx vite build` all clean; `src/routeTree.gen.ts` reverted via `git checkout --` after the build regenerated it, `.output`/`.wrangler` removed. **Not touched**: `site-shell.tsx`, `styles.css`, `ideas.functions.ts`, `ideas-shared.ts`, `validate-button.tsx`, or any other route — this commit changes exactly one file.
