/**
 * FEATURED BLUEPRINTS — the only place the homepage picks are defined.
 *
 * To change which ideas appear in the "Featured blueprints" strip on the
 * homepage, edit the array below and nothing else. Use real `idea_id` values
 * from the `ideas` table (format: IDEA-00000). Order here is the order shown.
 *
 * Any id that does not exist (or is not status='completed') is silently
 * skipped — the strip never renders fake data.
 */
export const FEATURED_IDEA_IDS: string[] = [
  "IDEA-00022", // Human Lens Media — trend 89
  "IDEA-00012", // Narrative Weave — trend 87
  "IDEA-00021", // Hidden Hand Channels — trend 86
];